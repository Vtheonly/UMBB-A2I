---
sources:
  - "[[02 Switch Configuration CLI]]"
  - "[[01 Introduction and Cabling]]"
  - "[[03 Router Basics and Gateways]]"
---
> [!question] The command `service password-encryption` is required to encrypt the `enable secret` password, as it is stored in plain text by default.
>> [!success]- Answer
>> False

> [!question] To save the configuration so it survives a reboot, you must use the command `copy running-config startup-config` in Privileged Exec mode.
>> [!success]- Answer
>> True

> [!question] When configuring `line console 0` or `line vty 0 4`, the `login` command is optional and the password will work without it.
>> [!success]- Answer
>> False

> [!question] The command `no shutdown` is required on a Router interface because ports are administratively down by default.
>> [!success]- Answer
>> True

> [!question] The `#` symbol in the command `banner motd # WARNING #` acts as a delimiter to mark the start and end of the message.
>> [!success]- Answer
>> True

> [!question] A "Straight-Through" cable is used to connect two Switches together because they are similar devices.
>> [!success]- Answer
>> False

> [!question] The `enable password` command creates an encrypted password, whereas `enable secret` creates a plain-text password.
>> [!success]- Answer
>> False

> [!question] A PC must have a "Default Gateway" IP configured to communicate with devices on a different network (e.g., the Internet).
>> [!success]- Answer
>> True

> [!question] The Console Cable (Blue Curve) is used to send network data and access the internet through the Switch.
>> [!success]- Answer
>> False

> [!question] In the command `line vty 0 4`, the numbers indicate that 5 simultaneous remote connections are allowed.
>> [!success]- Answer
>> True

> [!question] Which command is used to encrypt all current and future plain-text passwords in the configuration file?
> a) enable secret
> b) service password-encryption
> c) copy running-config startup-config
> d) crypto key generate
>> [!success]- Answer
>> b) service password-encryption

> [!question] What is the full command to save the current configuration from RAM to NVRAM?
> a) save config
> b) copy startup-config running-config
> c) copy running-config startup-config
> d) write memory ram
>> [!success]- Answer
>> c) copy running-config startup-config

> [!question] Which command allows you to configure remote access (Telnet) on a Cisco device?
> a) line console 0
> b) interface vlan 1
> c) line vty 0 4
> d) enable remote
>> [!success]- Answer
>> c) line vty 0 4

> [!question] When configuring a Router interface, which two commands are absolutely essential to make it functional?
> a) description and bandwidth
> b) ip address and no shutdown
> c) hostname and enable secret
> d) duplex full and speed 100
>> [!success]- Answer
>> b) ip address and no shutdown

> [!question] Which command sets a legal warning message that is displayed when a user connects to the device?
> a) hostname warning
> b) banner motd # message #
> c) description access restricted
> d) show version
>> [!success]- Answer
>> b) banner motd # message #

> [!question] If you set a password on `line console 0` but forget to type `login`, what happens?
> a) The switch will reject all connections.
> b) The switch will never ask for the password.
> c) The switch will use the default password "cisco".
> d) The switch will shut down the console port.
>> [!success]- Answer
>> b) The switch will never ask for the password.

> [!question] Which command allows you to enter Global Configuration Mode from Privileged Exec Mode?
> a) enable
> b) exit
> c) configure terminal
> d) interface fastEthernet
>> [!success]- Answer
>> c) configure terminal

> [!question] Which verification command displays the current active configuration, including unencrypted passwords?
> a) show startup-config
> b) show version
> c) show ip interface brief
> d) show running-config
>> [!success]- Answer
>> d) show running-config

> [!question] Which physical cable must be used to connect a PC to a Router for the very first configuration (no IP set yet)?
> a) Straight-Through Cable
> b) Cross-Over Cable
> c) Console Cable
> d) Fiber Optic Cable
>> [!success]- Answer
>> c) Console Cable

> [!question] What is the specific prompt for Interface Configuration Mode?
> a) Switch>
> b) Switch#
> c) Switch(config)#
> d) Switch(config-if)#
>> [!success]- Answer
>> d) Switch(config-if)#

> [!question] Match the long configuration command with its specific purpose.
>> [!example] Group A
>> a) service password-encryption
>> b) copy running-config startup-config
>> c) banner motd # Text #
>> d) enable secret cisco
>
>> [!example] Group B
>> n) Sets a banner message for legal warnings.
>> o) Hides/Encrypts all plain text passwords in the config file.
>> p) Saves the configuration from RAM to NVRAM.
>> q) Sets a strong, encrypted password for privileged mode.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> p)
>> c) -> n)
>> d) -> q)

> [!question] Match the Line/Interface command with its function.
>> [!example] Group A
>> a) line console 0
>> b) line vty 0 4
>> c) no shutdown
>
>> [!example] Group B
>> n) Activates a router interface (turns it "UP").
>> o) Enters configuration mode for physical access.
>> p) Enters configuration mode for remote (Telnet) access.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> p)
>> c) -> n)

> [!question] Match the verification command with the information it displays.
>> [!example] Group A
>> a) show running-config
>> b) show mac-address-table
>> c) show ip route
>
>> [!example] Group B
>> n) Displays the switch's knowledge of which device is on which port.
>> o) Displays the routing table and known networks.
>> p) Displays the configuration currently active in RAM.
>
>> [!success]- Answer
>> a) -> p)
>> b) -> n)
>> c) -> o)

> [!question] Match the prompt with the correct Cisco IOS mode.
>> [!example] Group A
>> a) Switch>
>> b) Switch#
>> c) Switch(config)#
>
>> [!example] Group B
>> n) Global Configuration Mode
>> o) User Exec Mode
>> p) Privileged Exec Mode
>
>> [!success]- Answer
>> a) -> o)
>> b) -> p)
>> c) -> n)

> [!question] Match the cabling type with the correct connection scenario.
>> [!example] Group A
>> a) Straight-Through Cable
>> b) Cross-Over Cable
>> c) Console Cable
>
>> [!example] Group B
>> n) PC to Switch (Different devices)
>> o) PC to Switch Console Port (Configuration)
>> p) Switch to Switch (Similar devices)
>
>> [!success]- Answer
>> a) -> n)
>> b) -> p)
>> c) -> o)

> [!question] Match the hardware device with its Layer/Function.
>> [!example] Group A
>> a) Hub
>> b) Switch
>> c) Router
>
>> [!example] Group B
>> n) Layer 2, uses MAC Address Table to filter traffic.
>> o) Layer 1, repeats data to all ports (floods).
>> p) Connects different networks and uses IP addresses.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)
>> c) -> p)

> [!question] Match the command element with its description.
>> [!example] Group A
>> a) login
>> b) exit
>> c) hostname
>
>> [!example] Group B
>> n) Sets the device's unique identity.
>> o) Moves you back one step in the CLI hierarchy.
>> p) Tells the device to actually check for a password on a line.
>
>> [!success]- Answer
>> a) -> p)
>> b) -> o)
>> c) -> n)

> [!question] Match the memory type with its content.
>> [!example] Group A
>> a) RAM
>> b) NVRAM
>
>> [!example] Group B
>> n) Stores the Startup Config (Permanent storage).
>> o) Stores the Running Config (Lost on power loss).
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)

> [!question] Match the IP concept with its definition.
>> [!example] Group A
>> a) IP Address
>> b) Subnet Mask
>> c) Default Gateway
>
>> [!example] Group B
>> n) The router IP used to exit the local network.
>> o) The logical identifier of the device.
>> p) Defines the size of the network.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> p)
>> c) -> n)

> [!question] Match the troubleshooting command with its primary use.
>> [!example] Group A
>> a) ping
>> b) tracert
>> c) show ip interface brief
>
>> [!example] Group B
>> n) Tests basic connectivity between two IPs.
>> o) Shows the path and hops taken to reach a destination.
>> p) Quickly checks if interfaces are UP/UP or DOWN.
>
>> [!success]- Answer
>> a) -> n)
>> b) -> o)
>> c) -> p)