---
sources:
  - "[[01 Introduction and Cabling]]"
  - "[[02 Switch Configuration CLI]]"
  - "[[03 Router Basics and Gateways]]"
---
---
sources:
  - "[[01 Introduction and Cabling]]"
  - "[[02 Switch Configuration CLI]]"
  - "[[03 Router Basics and Gateways]]"
---
> [!question] A Hub creates a separate collision domain for every port, making the network faster and more secure.
>> [!success]- Answer
>> False

> [!question] If you connect two Switches together, you should use a Cross-Over cable because they are similar devices.
>> [!success]- Answer
>> True

> [!question] The `enable secret` password is stored in plain text in the configuration file, making it easy to read if someone sees the file.
>> [!success]- Answer
>> False

> [!question] To configure a Router's interface, you must enter Global Configuration Mode and then Interface Configuration Mode.
>> [!success]- Answer
>> True

> [!question] A PC connected to a Switch needs a Default Gateway configured to communicate with another PC on the *same* local network.
>> [!success]- Answer
>> False

> [!question] The `banner motd` command is used to configure the time and date on the Cisco device.
>> [!success]- Answer
>> False

> [!question] The command `show ip interface brief` is useful for quickly checking if router interfaces are "UP" and have the correct IP addresses.
>> [!success]- Answer
>> True

> [!question] A Console connection (using a blue cable) allows you to send files and browse the internet through the router.
>> [!success]- Answer
>> False

> [!question] The `login` command is required on `line console 0` to force the device to prompt for the configured password.
>> [!success]- Answer
>> True

> [!question] Routers are used to connect different networks, such as connecting a LAN (192.168.1.x) to a WAN or another LAN (172.16.1.x).
>> [!success]- Answer
>> True

> [!question] Which command moves you from User Exec Mode (`>`) to Privileged Exec Mode (`#`)?
> a) configure terminal
> b) enable
> c) login
> d) exit
>> [!success]- Answer
>> b) enable

> [!question] Which device operates at Layer 1 and simply repeats/floods signals to all ports?
> a) Router
> b) Switch
> c) Hub
> d) Server
>> [!success]- Answer
>> c) Hub

> [!question] What is the correct command to set the device name to "Router1"?
> a) name Router1
> b) set hostname Router1
> c) hostname Router1
> d) device-id Router1
>> [!success]- Answer
>> c) hostname Router1

> [!question] Which command saves the current configuration to NVRAM so it is not lost when power is cut?
> a) copy running-config startup-config
> b) copy startup-config running-config
> c) save all
> d) write memory ram
>> [!success]- Answer
>> a) copy running-config startup-config

> [!question] Which cable is required to connect a PC's serial port to a Switch's console port?
> a) Straight-Through Cable
> b) Fiber Optic Cable
> c) Console Cable
> d) Cross-Over Cable
>> [!success]- Answer
>> c) Console Cable

> [!question] What does the command `no shutdown` do on a Router interface?
> a) It shuts down the interface.
> b) It restarts the router.
> c) It enables (turns on) the interface.
> d) It erases the configuration.
>> [!success]- Answer
>> c) It enables (turns on) the interface.

> [!question] Which verification command displays the MAC Address Table on a switch?
> a) show ip route
> b) show mac-address-table
> c) show interfaces
> d) show arp
>> [!success]- Answer
>> b) show mac-address-table

> [!question] If you want to prevent passwords from appearing in plain text in the `show run` output, which command should you use?
> a) enable secret
> b) service password-encryption
> c) banner motd
> d) secure-passwords
>> [!success]- Answer
>> b) service password-encryption

> [!question] Which CLI mode is indicated by the prompt `Switch(config-line)#`?
> a) Interface Configuration Mode
> b) Line Configuration Mode
> c) Global Configuration Mode
> d) User Exec Mode
>> [!success]- Answer
>> b) Line Configuration Mode

> [!question] What is the primary function of a Default Gateway on a PC?
> a) To assign an IP address to the PC.
> b) To route traffic destined for a different network/internet.
> c) To prevent collisions on the local network.
> d) To encrypt data sent to the switch.
>> [!success]- Answer
>> b) To route traffic destined for a different network/internet.

> [!question] Match the command with the correct CLI mode it is typed in.
>> [!example] Group A
>> a) enable
>> b) hostname S1
>> c) ip address 192.168.1.1 ...
>
>> [!example] Group B
>> n) Global Config Mode (config)#
>> o) Interface Config Mode (config-if)#
>> p) User Exec Mode (>)
>
>> [!success]- Answer
>> a) -> p)
>> b) -> n)
>> c) -> o)

> [!question] Match the cabling scenario with the correct cable type.
>> [!example] Group A
>> a) PC to Switch
>> b) Switch to Switch
>> c) PC to Router (Direct Connection)
>
>> [!example] Group B
>> n) Cross-Over Cable
>> o) Straight-Through Cable
>> p) Cross-Over Cable (Different layers/Direct)
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)
>> c) -> p)

> [!question] Match the password command with its specific use.
>> [!example] Group A
>> a) enable secret
>> b) line console 0 + password
>> c) line vty 0 4 + password
>
>> [!example] Group B
>> n) Secures physical access to the device.
>> o) Secures remote network access (Telnet).
>> p) Secures administrative privileges (Super-user).
>
>> [!success]- Answer
>> a) -> p)
>> b) -> n)
>> c) -> o)

> [!question] Match the show command with the information it verifies.
>> [!example] Group A
>> a) show startup-config
>> b) show version
>> c) show ip route
>
>> [!example] Group B
>> n) Hardware info, IOS version, and uptime.
>> o) The routing table (network paths).
>> p) The saved configuration in NVRAM.
>
>> [!success]- Answer
>> a) -> p)
>> b) -> n)
>> c) -> o)

> [!question] Match the device with its OS layer/behavior.
>> [!example] Group A
>> a) Hub
>> b) Switch
>> c) Router
>
>> [!example] Group B
>> n) "Smart" device, breaks collision domains (Layer 2).
>> o) "Dumb" device, single collision domain (Layer 1).
>> p) Connects networks, uses IP addresses (Layer 3).
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)
>> c) -> p)

> [!question] Match the Prompt with the command used to exit it.
>> [!example] Group A
>> a) Switch(config)#
>> b) Switch#
>> c) Switch>
>
>> [!example] Group B
>> n) enable
>> o) exit (to get to Privileged)
>> p) disable (to get to User)
>
>> [!success]- Answer
>> a) -> o)
>> b) -> p)
>> c) -> n)

> [!question] Match the simulation concept with its meaning.
>> [!example] Group A
>> a) PDU (Envelope)
>> b) Realtime Mode
>> c) Simulation Mode
>
>> [!example] Group B
>> n) Network runs at normal speed, immediate results.
>> o) Step-by-step packet analysis.
>> p) A unit of data (packet/frame) being sent.
>
>> [!success]- Answer
>> a) -> p)
>> b) -> n)
>> c) -> o)

> [!question] Match the IP configuration field with its purpose.
>> [!example] Group A
>> a) IP Address
>> b) Subnet Mask
>> c) Default Gateway
>
>> [!example] Group B
>> n) Determines which part of the IP is the network vs host.
>> o) The router's internal address for exiting the LAN.
>> p) The unique identity of the host on the network.
>
>> [!success]- Answer
>> a) -> p)
>> b) -> n)
>> c) -> o)

> [!question] Match the port status with the cause.
>> [!example] Group A
>> a) Status: Up / Protocol: Up
>> b) Status: Administratively Down
>> c) Status: Down / Protocol: Down
>
>> [!example] Group B
>> n) Cable is unplugged or device is off.
>> o) Interface is working correctly.
>> p) `no shutdown` command was not entered.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> p)
>> c) -> n)

> [!question] Match the command shortcut with the full command.
>> [!example] Group A
>> a) config t
>> b) wr
>> c) no shut
>
>> [!example] Group B
>> n) copy running-config startup-config (write memory)
>> o) no shutdown
>> p) configure terminal
>
>> [!success]- Answer
>> a) -> p)
>> b) -> n)
>> c) -> o)