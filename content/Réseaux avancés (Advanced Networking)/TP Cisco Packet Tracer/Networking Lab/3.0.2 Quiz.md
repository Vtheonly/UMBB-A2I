
---
sources:
  - "[[01 Introduction and Cabling]]"
  - "[[02 Switch Configuration CLI]]"
  - "[[03 Router Basics and Gateways]]"
---
> [!question] A Hub is considered a "smart" device because it uses a MAC Address Table to filter traffic.
>> [!success]- Answer
>> False

> [!question] To connect two PCs directly to each other without a switch, you must use a Cross-Over cable.
>> [!success]- Answer
>> True

> [!question] The "Default Gateway" IP address configured on a PC must match the IP address of the Router interface attached to that network.
>> [!success]- Answer
>> True

> [!question] The command `show ip route` is primarily used on a Switch to see which computers are connected to which port.
>> [!success]- Answer
>> False

> [!question] In Cisco IOS, the `running-config` is stored in RAM and will be lost if the device loses power without saving.
>> [!success]- Answer
>> True

> [!question] The orange dots seen on switch connections in Packet Tracer indicate that the Spanning Tree Protocol (STP) is checking for loops.
>> [!success]- Answer
>> True

> [!question] Telnet (configured via `line vty`) is a secure protocol that automatically encrypts all data and passwords.
>> [!success]- Answer
>> False

> [!question] On a Router, the interface is enabled by default and does not require the `no shutdown` command.
>> [!success]- Answer
>> False

> [!question] A Switch breaks up collision domains, meaning collisions do not occur between devices on different ports.
>> [!success]- Answer
>> True

> [!question] The Console cable connects to the Ethernet port of the PC to provide network access.
>> [!success]- Answer
>> False

> [!question] Which device is used to connect **different** networks (e.g., 192.168.1.0 and 172.16.1.0) together?
> a) Switch
> b) Hub
> c) Router
> d) Repeater
>> [!success]- Answer
>> c) Router

> [!question] Which command is used to display the status of interfaces (Up/Down) and their IP addresses?
> a) show ip route
> b) show version
> c) show run
> d) show ip interface brief
>> [!success]- Answer
>> d) show ip interface brief

> [!question] Which cable should be used to connect a PC to a Switch?
> a) Cross-Over Cable
> b) Straight-Through Cable
> c) Console Cable
> d) Coaxial Cable
>> [!success]- Answer
>> b) Straight-Through Cable

> [!question] What is the correct command sequence to assign an IP address to a router interface?
> a) ip address 192.168.1.1
> b) ip address 192.168.1.1 255.255.255.0
> c) set ip 192.168.1.1 mask 255.255.255.0
> d) interface ip 192.168.1.1
>> [!success]- Answer
>> b) ip address 192.168.1.1 255.255.255.0

> [!question] Which mode allows you to execute the `copy running-config startup-config` command?
> a) User Exec Mode (>)
> b) Global Configuration Mode ((config)#)
> c) Privileged Exec Mode (#)
> d) Interface Configuration Mode ((config-if)#)
>> [!success]- Answer
>> c) Privileged Exec Mode (#)

> [!question] What is the function of the command `service password-encryption`?
> a) It creates a secret password for the enable mode.
> b) It encrypts all current and future plain-text passwords in the config.
> c) It allows SSH connections.
> d) It saves the configuration to NVRAM.
>> [!success]- Answer
>> b) It encrypts all current and future plain-text passwords in the config.

> [!question] If a PC cannot ping a device on a remote network, which configuration is most likely missing or incorrect on the PC?
> a) Subnet Mask
> b) DNS Server
> c) Default Gateway
> d) MAC Address
>> [!success]- Answer
>> c) Default Gateway

> [!question] Which command is used to configure the name of the device?
> a) name
> b) hostname
> c) title
> d) identity
>> [!success]- Answer
>> b) hostname

> [!question] When configuring `line console 0`, which command ensures the switch actually checks for the password?
> a) enable
> b) secret
> c) login
> d) activate
>> [!success]- Answer
>> c) login

> [!question] Which utility is used to trace the path a packet takes to reach a destination?
> a) ping
> b) tracert
> c) ipconfig
> d) telnet
>> [!success]- Answer
>> b) tracert

> [!question] Match the device with its primary function.
>> [!example] Group A
>> a) Hub
>> b) Switch
>> c) Router
>
>> [!example] Group B
>> n) Learns MAC addresses to forward data only to the destination.
>> o) Connects different networks and routes packets based on IP.
>> p) Repeats signals to all ports (dumb device).
>
>> [!success]- Answer
>> a) -> p)
>> b) -> n)
>> c) -> o)

> [!question] Match the cable type with the devices it connects.
>> [!example] Group A
>> a) Straight-Through
>> b) Cross-Over
>> c) Console
>
>> [!example] Group B
>> n) Switch to Switch.
>> o) PC to Switch.
>> p) PC (Serial) to Router (Console).
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)
>> c) -> p)

> [!question] Match the CLI prompt with the mode name.
>> [!example] Group A
>> a) Router(config)#
>> b) Router#
>> c) Router(config-if)#
>
>> [!example] Group B
>> n) Privileged Exec Mode
>> o) Interface Configuration Mode
>> p) Global Configuration Mode
>
>> [!success]- Answer
>> a) -> p)
>> b) -> n)
>> c) -> o)

> [!question] Match the command with the configuration task.
>> [!example] Group A
>> a) line vty 0 4
>> b) enable secret cisco
>> c) banner motd # Hi #
>
>> [!example] Group B
>> n) Configuring a warning message.
>> o) Securing remote access (Telnet).
>> p) Securing the privileged exec mode.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> p)
>> c) -> n)

> [!question] Match the memory type with the file it holds.
>> [!example] Group A
>> a) RAM
>> b) NVRAM
>
>> [!example] Group B
>> n) running-config
>> o) startup-config
>
>> [!success]- Answer
>> a) -> n)
>> b) -> o)

> [!question] Match the address type with its layer/device.
>> [!example] Group A
>> a) MAC Address
>> b) IP Address
>> c) Port Number
>
>> [!example] Group B
>> n) Used by Switches (Layer 2).
>> o) Used by Routers (Layer 3).
>> p) Used by Applications (Layer 4) - *Note: Context implied by Telnet/Services*.
>
>> [!success]- Answer
>> a) -> n)
>> b) -> o)
>> c) -> p)

> [!question] Match the interface command with its purpose.
>> [!example] Group A
>> a) no shutdown
>> b) ip address
>> c) exit
>
>> [!example] Group B
>> n) Assigns the logical ID to the port.
>> o) Turns the interface ON.
>> p) Leaves the current mode.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)
>> c) -> p)

> [!question] Match the troubleshooting tool with its output.
>> [!example] Group A
>> a) ping
>> b) show mac-address-table
>> c) show ip route
>
>> [!example] Group B
>> n) "Gateway of last resort is not set..."
>> o) "Reply from 192.168.1.1: bytes=32..."
>> p) "Dynamic Fa0/1 0040.0b8c.d690"
>
>> [!success]- Answer
>> a) -> o)
>> b) -> p)
>> c) -> n)

> [!question] Match the Protocol/Service with its description.
>> [!example] Group A
>> a) Telnet
>> b) STP
>> c) ARP
>
>> [!example] Group B
>> n) Prevents loops in a switched network (Orange/Green dots).
>> o) Resolves IP addresses to MAC addresses (first packet flood).
>> p) Allows remote command-line management of devices.
>
>> [!success]- Answer
>> a) -> p)
>> b) -> n)
>> c) -> o)

> [!question] Match the physical port with its usage.
>> [!example] Group A
>> a) FastEthernet 0/0
>> b) Console Port
>> c) RS-232
>
>> [!example] Group B
>> n) Serial port on the PC used for configuration.
>> o) Management port on the Cisco device.
>> p) Network port used for sending data.
>
>> [!success]- Answer
>> a) -> p)
>> b) -> o)
>> c) -> n)
