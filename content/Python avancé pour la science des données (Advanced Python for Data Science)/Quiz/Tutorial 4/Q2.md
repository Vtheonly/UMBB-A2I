---
sources:
  - "[[Tutorial 04 Essential Python Libraries]]"
---

> [!question] In NumPy, `np.random.randint(0, 21)` includes the number 21 in the possible outcomes.
>> [!success]- Answer
>> False

> [!question] The function `np.argmax()` returns the coordinates as a tuple (row, column) by default.
>> [!success]- Answer
>> False

> [!question] You can pass a standard Python list of lists directly into `np.array()` to create a matrix.
>> [!success]- Answer
>> True

> [!question] `np.sum(boolean_mask)` calculates the sum of the values satisfying the condition, not the count of them.
>> [!success]- Answer
>> False

> [!question] In the context of `grades.mean(axis=1)`, the operation collapses the columns to produce one result per row (per student).
>> [!success]- Answer
>> True

> [!question] NumPy arrays occupy non-contiguous blocks of memory, similar to Python lists, which makes them flexible but slow.
>> [!success]- Answer
>> False

> [!question] The code `grades[:, 0]` selects the first column (Module 0) for all students.
>> [!success]- Answer
>> True

> [!question] `np.vstack` is used to stack arrays horizontally (adding more columns).
>> [!success]- Answer
>> False

> [!question] `np.percentile(data, [25, 75])` returns the values corresponding to the 1st and 3rd quartiles.
>> [!success]- Answer
>> True

> [!question] Using standard Python logical operators like `and` or `or` on NumPy arrays will result in a `ValueError`.
>> [!success]- Answer
>> True

> [!question] What does the code `np.argsort(array)[-5:]` do?
> a) It returns the 5 smallest values in the array.
> b) It returns the indices of the 5 largest values (sorted ascending).
> c) It returns the first 5 elements of the array.
> d) It calculates the mean of the last 5 elements.
>> [!success]- Answer
>> b) It returns the indices of the 5 largest values (sorted ascending).

> [!question] Why do we add `+ 1` to `day_idx` in the Temperature exercise (e.g., `day_idx + 1`)?
> a) Because Python is 1-indexed.
> b) To correct for a timezone difference.
> c) Because Python indices start at 0, but "Days" usually start at 1.
> d) To include the header row.
>> [!success]- Answer
>> c) Because Python indices start at 0, but "Days" usually start at 1.

> [!question] In the expression `grades[mask_range]`, what is the role of `mask_range`?
> a) It is a list of integers specifying which rows to keep.
> b) It is a boolean array used to filter elements where the value is True.
> c) It is a string defining the condition.
> d) It is the shape of the new array.
>> [!success]- Answer
>> b) It is a boolean array used to filter elements where the value is True.

> [!question] Which function returns the flat index of the global minimum value in an array?
> a) `np.min()`
> b) `np.argmin()`
> c) `np.argsort()`
> d) `np.where()`
>> [!success]- Answer
>> b) `np.argmin()`

> [!question] How many elements does `np.random.randint(0, 21, size=(100, 5))` generate in total?
> a) 100
> b) 105
> c) 500
> d) 21
>> [!success]- Answer
>> c) 500

> [!question] What is the output of `np.where(condition)`?
> a) A boolean array.
> b) A tuple of index arrays indicating where the condition is True.
> c) A new array with values replaced by 0.
> d) The sum of True values.
>> [!success]- Answer
>> b) A tuple of index arrays indicating where the condition is True.

> [!question] Which Matplotlib parameter was used to color the boxes in the boxplot?
> a) `fill=True`
> b) `color='blue'`
> c) `patch_artist=True`
> d) `style='filled'`
>> [!success]- Answer
>> c) `patch_artist=True`

> [!question] In the code `T[:, 1]`, what does the `1` refer to?
> a) The first row.
> b) The second row.
> c) The first column.
> d) The second column.
>> [!success]- Answer
>> d) The second column.

> [!question] What is the primary advantage of NumPy arrays over Python lists mentioned in the text?
> a) They can hold different data types in one array.
> b) They are vectorized and memory efficient.
> c) They have built-in SQL capabilities.
> d) They are easier to read.
>> [!success]- Answer
>> b) They are vectorized and memory efficient.

> [!question] Which NumPy function is used to calculate the spread or dispersion of data?
> a) `np.mean`
> b) `np.median`
> c) `np.std`
> d) `np.sum`
>> [!success]- Answer
>> c) `np.std`

> [!question] Match the Slicing/Indexing syntax to its meaning.
>> [!example] Group A
>> a) `array[::-1]`
>> b) `array[:, 0]`
>> c) `array[0, :]`
>
>> [!example] Group B
>> n) First Row (All columns).
>> o) First Column (All rows).
>> p) Reverse the array.
>
>> [!success]- Answer
>> a) -> p)
>> b) -> o)
>> c) -> n)

> [!question] Match the Statistical Metric to its Pandas/NumPy equivalent.
>> [!example] Group A
>> a) 50th Percentile
>> b) Average
>> c) Max - Min (conceptually)
>
>> [!example] Group B
>> n) `np.mean()`
>> o) Range (Global Max vs Global Min).
>> p) `np.median()`
>
>> [!success]- Answer
>> a) -> p)
>> b) -> n)
>> c) -> o)

> [!question] Match the Logic to the Code Symbol.
>> [!example] Group A
>> a) AND
>> b) OR
>> c) Greater Than or Equal To
>
>> [!example] Group B
>> n) `>=`
>> o) `&`
>> p) `|`
>
>> [!success]- Answer
>> a) -> o)
>> b) -> p)
>> c) -> n)

> [!question] Match the Function to its application in the exercises.
>> [!example] Group A
>> a) `np.vstack`
>> b) `np.maximum`
>> c) `np.percentile`
>
>> [!example] Group B
>> n) Comparing Alger vs Oran temps.
>> o) Analyzing grade distribution (25% vs 75%).
>> p) Adding an average row to the table.
>
>> [!success]- Answer
>> a) -> p)
>> b) -> n)
>> c) -> o)

> [!question] Match the NumPy attribute to its description.
>> [!example] Group A
>> a) `.shape`
>> b) `.T` (implied in general usage, or context of rows/cols)
>> c) `.min()`
>
>> [!example] Group B
>> n) Returns the smallest value.
>> o) Returns dimensions (Rows, Columns).
>> p) Transposes the array (swaps axes).
>
>> [!success]- Answer
>> a) -> o)
>> b) -> p)
>> c) -> n)

> [!question] Match the visual plot to the insight it provides.
>> [!example] Group A
>> a) Box Plot
>> b) Bar Chart
>
>> [!example] Group B
>> n) Comparing the exact average value of each module.
>> o) Detecting skewness and outliers in grades.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)

> [!question] Match the Boolean Masking steps.
>> [!example] Group A
>> a) `mask = grades > 15`
>> b) `np.sum(mask)`
>> c) `grades[mask]`
>
>> [!example] Group B
>> n) Counts how many items satisfy the condition.
>> o) Creates a True/False array.
>> p) Extracts the actual values satisfying the condition.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)
>> c) -> p)

> [!question] Match the Axis choice to the operation in the Grades exercise.
>> [!example] Group A
>> a) axis=0
>> b) axis=1
>
>> [!example] Group B
>> n) Calculate average grade for a specific Student.
>> o) Calculate average grade for a specific Module.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)

> [!question] Match the variable name to its content in Exercise 1.
>> [!example] Group A
>> a) `T`
>> b) `cities`
>> c) `comparison`
>
>> [!example] Group B
>> n) A list of strings ['Alger', 'Annaba', 'Oran'].
>> o) An array containing the max of Alger vs Oran.
>> p) The main data array of temperatures.
>
>> [!success]- Answer
>> a) -> p)
>> b) -> n)
>> c) -> o)

> [!question] Match the Random generation argument to its meaning.
>> [!example] Group A
>> a) `low=0`
>> b) `high=21`
>> c) `size=(100, 5)`
>
>> [!example] Group B
>> n) The exclusive upper bound.
>> o) The dimensions of the output array.
>> p) The inclusive lower bound.
>
>> [!success]- Answer
>> a) -> p)
>> b) -> n)
>> c) -> o)