---
sources:
  - "[[Tutorial 04 Essential Python Libraries]]"
---

> [!question] NumPy arrays are generally slower than standard Python lists because they are built on C-based arrays.
>> [!success]- Answer
>> False

> [!question] In NumPy, `axis=0` typically acts on columns (collapsing the rows downward), while `axis=1` acts on rows (collapsing the columns sideways).
>> [!success]- Answer
>> True

> [!question] When performing boolean masking in NumPy (e.g., filtering grades), you must use the Python keywords `and` and `or`.
>> [!success]- Answer
>> False

> [!question] The function `np.argmax(array)` returns the actual maximum value contained in the array.
>> [!success]- Answer
>> False

> [!question] `np.random.seed(101)` ensures that random number generation is reproducible (i.e., it produces the same sequence of numbers every time the code runs).
>> [!success]- Answer
>> True

> [!question] The function `np.argsort()` returns the indices that would sort an array, rather than the sorted values themselves.
>> [!success]- Answer
>> True

> [!question] Vectorization allows mathematical operations to be performed on entire arrays at once, eliminating the need for explicit `for` loops.
>> [!success]- Answer
>> True

> [!question] `np.maximum(Array1, Array2)` calculates the single highest value found across both arrays combined.
>> [!success]- Answer
>> False

> [!question] `np.unravel_index` is used to convert a flat index (returned by argmax) back into a tuple of (Row, Column) coordinates.
>> [!success]- Answer
>> True

> [!question] `np.random.randint(0, 21)` generates integers from 0 up to and including 21.
>> [!success]- Answer
>> False

> [!question] Which NumPy concept describes the ability to perform math on whole arrays without writing loops?
> a) Broadcasting
> b) Vectorization
> c) Indexing
> d) Compiling
>> [!success]- Answer
>> b) Vectorization

> [!question] If you have a 2D array of Student Grades (Rows=Students, Cols=Modules), which axis would you use to calculate the average grade for **each student**?
> a) axis=0
> b) axis=1
> c) axis=-1
> d) axis=None
>> [!success]- Answer
>> b) axis=1

> [!question] What is the correct syntax to create a boolean mask for grades greater than 10 AND less than 15?
> a) `(grades > 10) and (grades < 15)`
> b) `grades > 10 & grades < 15` (without parentheses)
> c) `(grades > 10) & (grades < 15)`
> d) `(grades > 10) && (grades < 15)`
>> [!success]- Answer
>> c) `(grades > 10) & (grades < 15)`

> [!question] What does `np.percentile(data, 50)` calculate?
> a) The average.
> b) The mode.
> c) The median.
> d) The standard deviation.
>> [!success]- Answer
>> c) The median.

> [!question] In the Temperature exercise, why did we use `np.vstack`?
> a) To remove a column from the array.
> b) To calculate the variance.
> c) To add a new row (Averages) to the bottom of the existing table.
> d) To sort the temperatures.
>> [!success]- Answer
>> c) To add a new row (Averages) to the bottom of the existing table.

> [!question] Which visualization was used to detect skewness and outliers in the student grades distribution?
> a) Scatter Plot
> b) Box Plot
> c) Bar Chart
> d) Pie Chart
>> [!success]- Answer
>> b) Box Plot

> [!question] What is the specific purpose of `np.where(condition)` in the tutorial code?
> a) It returns the values that satisfy the condition.
> b) It returns the indices (positions) of elements that satisfy the condition.
> c) It counts how many elements satisfy the condition.
> d) It deletes elements that do not satisfy the condition.
>> [!success]- Answer
>> b) It returns the indices (positions) of elements that satisfy the condition.

> [!question] If `grades` is an array of shape (100, 5), what does `grades.shape` return?
> a) 100
> b) 5
> c) 500
> d) (100, 5)
>> [!success]- Answer
>> d) (100, 5)

> [!question] To find the top 5 students based on their average, the tutorial used `np.argsort` followed by slicing. Which slice gets the last 5 items in reverse order?
> a) `[:5]`
> b) `[-5:]`
> c) `[-5:][::-1]`
> d) `[5::-1]`
>> [!success]- Answer
>> c) `[-5:][::-1]`

> [!question] Which comparison operator allows you to check if *all* elements in a row satisfy a condition (e.g., "Student has ALL grades > 15")?
> a) `np.any()`
> b) `np.all()`
> c) `np.sum()`
> d) `np.where()`
>> [!success]- Answer
>> b) `np.all()`

> [!question] Match the NumPy function to its return value.
>> [!example] Group A
>> a) np.argmax()
>> b) np.max()
>> c) np.argsort()
>
>> [!example] Group B
>> n) The highest value in the array.
>> o) The indices that would sort the array.
>> p) The index of the highest value (flat).
>
>> [!success]- Answer
>> a) -> p)
>> b) -> n)
>> c) -> o)

> [!question] Match the Axis parameter to the direction of operation.
>> [!example] Group A
>> a) axis=0
>> b) axis=1
>
>> [!example] Group B
>> n) Across rows (Horizontal / Left-to-Right).
>> o) Down columns (Vertical / Top-to-Bottom).
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)

> [!question] Match the Boolean Operator to the correct usage.
>> [!example] Group A
>> a) &
>> b) |
>> c) ~ (Tilde)
>
>> [!example] Group B
>> n) Bitwise OR (used for 'or' logic in masks).
>> o) Bitwise NOT (inverts the mask).
>> p) Bitwise AND (used for 'and' logic in masks).
>
>> [!success]- Answer
>> a) -> p)
>> b) -> n)
>> c) -> o)

> [!question] Match the Statistical function to its description.
>> [!example] Group A
>> a) np.std()
>> b) np.var()
>> c) np.median()
>
>> [!example] Group B
>> n) The middle value of sorted data.
>> o) Standard Deviation (spread).
>> p) Variance (average squared deviation).
>
>> [!success]- Answer
>> a) -> o)
>> b) -> p)
>> c) -> n)

> [!question] Match the Array manipulation function to its action.
>> [!example] Group A
>> a) np.vstack()
>> b) np.unravel_index()
>> c) np.maximum()
>
>> [!example] Group B
>> n) Compares two arrays element-wise and takes the larger value.
>> o) Stacks arrays vertically (row-wise).
>> p) Converts a flat index into coordinate tuple.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> p)
>> c) -> n)

> [!question] Match the Tutorial Exercise to the data shape used.
>> [!example] Group A
>> a) Temperature Analysis
>> b) Student Grades
>
>> [!example] Group B
>> n) (100, 5) -> 100 Students, 5 Modules.
>> o) (7, 3) -> 7 Days, 3 Cities.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)

> [!question] Match the Code Snippet to the result type.
>> [!example] Group A
>> a) `grades > 15`
>> b) `grades[grades > 15]`
>> c) `np.sum(grades > 15)`
>
>> [!example] Group B
>> n) An array of Boolean values (True/False).
>> o) An integer representing the count of True values.
>> p) A flat array containing only the values greater than 15.
>
>> [!success]- Answer
>> a) -> n)
>> b) -> p)
>> c) -> o)

> [!question] Match the Matplotlib function to its use in the NumPy tutorial.
>> [!example] Group A
>> a) plt.bar()
>> b) plt.boxplot()
>
>> [!example] Group B
>> n) Visualizing the Average Grade per Module.
>> o) Visualizing the Distribution (Quartiles/Outliers) per Module.
>
>> [!success]- Answer
>> a) -> n)
>> b) -> o)

> [!question] Match the Random function to its description.
>> [!example] Group A
>> a) np.random.seed()
>> b) np.random.randint()
>
>> [!example] Group B
>> n) Generates random integers within a range.
>> o) Initializes the random number generator to a fixed state.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)

> [!question] Match the advanced indexing technique to the example.
>> [!example] Group A
>> a) Boolean Masking
>> b) Slicing
>
>> [!example] Group B
>> n) `grades[:5]`
>> o) `grades[grades < 10]`
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)