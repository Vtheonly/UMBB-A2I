---
sources:
  - "[[Tutorial 04 Project Monthly Sales Analysis]]"
---

> [!question] The function `df.pct_change()` calculates the percentage difference between the current element and the previous one (Row-wise by default).
>> [!success]- Answer
>> True

> [!question] A Heatmap is best used to show the exact numerical distribution, quartiles, and outliers of a dataset.
>> [!success]- Answer
>> False

> [!question] In a Stacked Bar Chart, the height of the entire bar represents the sum of all categories (Products) for that period.
>> [!success]- Answer
>> True

> [!question] The method `df.idxmax(axis=1)` returns the highest numerical value in the row.
>> [!success]- Answer
>> False

> [!question] Setting the index to a Date column (`df.set_index('Date')`) is crucial for effective Time Series manipulation in Pandas.
>> [!success]- Answer
>> True

> [!question] The code `df.to_csv('data/output.csv')` appends data to an existing file rather than overwriting it.
>> [!success]- Answer
>> False

> [!question] In Seaborn, `annot=True` inside a heatmap function writes the data values inside each colored cell.
>> [!success]- Answer
>> True

> [!question] A "Large Box" in a boxplot indicates that the sales data for that product is very consistent and stable.
>> [!success]- Answer
>> False

> [!question] The `freq='M'` parameter in `pd.date_range` generates dates corresponding to the start of each month.
>> [!success]- Answer
>> False

> [!question] Pandas Pivot Tables are used to summarize and aggregate data, such as calculating average sales per Quarter.
>> [!success]- Answer
>> True

> [!question] Which Pandas method is used to determine the "Best Performing Product" (the column name) for each month?
> a) `df.max()`
> b) `df.idxmax()`
> c) `df.argmax()`
> d) `df.sort_values()`
>> [!success]- Answer
>> b) `df.idxmax()`

> [!question] Why do we typically fill NaNs (`fillna(0)`) after running `pct_change()`?
> a) Because the calculation is incorrect.
> b) Because the first row has no previous data to compare with, resulting in NaN.
> c) Because Pandas generates NaNs for negative growth.
> d) To remove outliers.
>> [!success]- Answer
>> b) Because the first row has no previous data to compare with, resulting in NaN.

> [!question] What does the `.T` attribute do to a DataFrame (used before creating the Heatmap)?
> a) It truncates the data.
> b) It translates the data.
> c) It transposes the data (swaps rows and columns).
> d) It transforms the data into a list.
>> [!success]- Answer
>> c) It transposes the data (swaps rows and columns).

> [!question] Which visualization is described as showing the "Story" (trends/ups and downs) over time?
> a) Boxplot
> b) Line Chart
> c) Histogram
> d) Pie Chart
>> [!success]- Answer
>> b) Line Chart

> [!question] In the Stacked Bar Chart code, what does the `stacked=True` parameter achieve?
> a) It places bars side-by-side.
> b) It places bars on top of each other.
> c) It creates a 3D effect.
> d) It sorts the bars by height.
>> [!success]- Answer
>> b) It places bars on top of each other.

> [!question] How is the "Quarter" column created in the Feature Engineering step?
> a) `df.index.quarter`
> b) `df.date.month / 3`
> c) `df.groupby('Month')`
> d) `pd.to_quarter(df)`
>> [!success]- Answer
>> a) `df.index.quarter`

> [!question] Which Seaborn palette is used to indicate "Darker colors = Higher sales" in the tutorial?
> a) `grayscale`
> b) `YlGnBu` (Yellow-Green-Blue)
> c) `rainbow`
> d) `pastel`
>> [!success]- Answer
>> b) `YlGnBu` (Yellow-Green-Blue)

> [!question] What does a negative value in the `MoM_Growth` column indicate?
> a) The sales increased.
> b) The sales were zero.
> c) The sales declined compared to the previous month.
> d) The data is missing.
>> [!success]- Answer
>> c) The sales declined compared to the previous month.

> [!question] Which Matplotlib function is used to add an arrow pointing to the "Best Month"?
> a) `plt.arrow()`
> b) `plt.pointer()`
> c) `plt.annotate()`
> d) `plt.text()`
>> [!success]- Answer
>> c) `plt.annotate()`

> [!question] Based on the tutorial's conclusion, which Product was the "Cash Cow" (Market Leader)?
> a) Product A
> b) Product B
> c) Product C
> d) Product D
>> [!success]- Answer
>> a) Product A

> [!question] Match the Visualization to its primary purpose in the project.
>> [!example] Group A
>> a) Line Chart
>> b) Stacked Bar Chart
>> c) Heatmap
>> d) Boxplot
>
>> [!example] Group B
>> n) Shows total volume and composition per month.
>> o) Shows distribution, stability, and range of sales.
>> p) Shows trends and sequence over time.
>> q) Shows intensity and seasonality in a grid format.
>
>> [!success]- Answer
>> a) -> p)
>> b) -> n)
>> c) -> q)
>> d) -> o)

> [!question] Match the Pandas method to its calculation.
>> [!example] Group A
>> a) `sum(axis=1)`
>> b) `mean(axis=1)`
>> c) `pct_change()`
>
>> [!example] Group B
>> n) Calculates the average sales across all products for that month.
>> o) Calculates the Month-over-Month growth rate.
>> p) Calculates the total revenue for the month (Horizontal sum).
>
>> [!success]- Answer
>> a) -> p)
>> b) -> n)
>> c) -> o)

> [!question] Match the Plotting parameter to its effect.
>> [!example] Group A
>> a) `figsize=(12, 6)`
>> b) `marker='o'`
>> c) `cmap='viridis'`
>
>> [!example] Group B
>> n) Sets the color scheme for the plot.
>> o) Draws dots at each data point on the line.
>> p) Sets the width and height of the figure.
>
>> [!success]- Answer
>> a) -> p)
>> b) -> o)
>> c) -> n)

> [!question] Match the Business Insight to the data evidence.
>> [!example] Group A
>> a) High Variance/Volatility
>> b) Low Variance/Consistent
>> c) Seasonality
>
>> [!example] Group B
>> n) Small box in the boxplot.
>> o) Large box/long whiskers in the boxplot.
>> p) Dark vertical stripes in the heatmap during specific months.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)
>> c) -> p)

> [!question] Match the Time Series concept to the code.
>> [!example] Group A
>> a) Frequency
>> b) Index
>> c) Quarter
>
>> [!example] Group B
>> n) `df.set_index('Date')`
>> o) `df.index.quarter`
>> p) `freq='M'`
>
>> [!success]- Answer
>> a) -> p)
>> b) -> n)
>> c) -> o)

> [!question] Match the Pandas/Seaborn function to the output file type or display.
>> [!example] Group A
>> a) `to_csv()`
>> b) `plt.show()`
>> c) `display()`
>
>> [!example] Group B
>> n) Renders the plot to the screen.
>> o) Shows the DataFrame in a nice format in Jupyter.
>> p) Saves the DataFrame to a text file on the disk.
>
>> [!success]- Answer
>> a) -> p)
>> b) -> n)
>> c) -> o)

> [!question] Match the `idxmax`/`idxmin` function to its result.
>> [!example] Group A
>> a) `idxmax(axis=1)`
>> b) `idxmin(axis=1)`
>
>> [!example] Group B
>> n) Returns the column name of the lowest value (Worst Product).
>> o) Returns the column name of the highest value (Best Product).
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)

> [!question] Match the Feature Engineering step to the column name created.
>> [!example] Group A
>> a) MoM_Growth
>> b) Total_Sales
>> c) Max_Sales_Product
>
>> [!example] Group B
>> n) Derived from `df.sum(axis=1)`.
>> o) Derived from `df.idxmax(axis=1)`.
>> p) Derived from `df.pct_change()`.
>
>> [!success]- Answer
>> a) -> p)
>> b) -> n)
>> c) -> o)