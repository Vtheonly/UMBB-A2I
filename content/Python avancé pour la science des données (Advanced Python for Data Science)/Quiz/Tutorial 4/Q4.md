---
sources:
  - "[[Tutorial 04 Project Monthly Sales Analysis]]"
---

> [!question] The code `df.sum(axis=1)` calculates the total sales for each product across the entire year (Column Sum).
>> [!success]- Answer
>> False

> [!question] When using `df.plot(kind='bar')`, the X-axis labels are automatically taken from the DataFrame's index (the Date).
>> [!success]- Answer
>> True

> [!question] The `pivot_table` function was used to aggregate monthly data into Quarterly averages (Q1, Q2, Q3, Q4).
>> [!success]- Answer
>> True

> [!question] In the `annotate` function, `xy` represents the position of the text label, and `xytext` represents the point being pointed to on the graph.
>> [!success]- Answer
>> False

> [!question] The `.map()` function was used to convert integer quarter numbers (1, 2, 3, 4) into string labels ('Q1', 'Q2', etc.).
>> [!success]- Answer
>> True

> [!question] `df.T` is a permanent operation that modifies the DataFrame in place without needing to assign it to a variable or pass it to a function.
>> [!success]- Answer
>> False

> [!question] In the boxplot analysis, the "whiskers" extending from the box represent the full range of data, excluding potential outliers.
>> [!success]- Answer
>> True

> [!question] The `argmax()` function used on the `Total_Sales` column returns the *value* of the highest sale (e.g., 5000), not the index.
>> [!success]- Answer
>> False

> [!question] You can perform arithmetic operations (like multiplication) directly on a Pandas Series, such as `df['col'] * 100`.
>> [!success]- Answer
>> True

> [!question] The `fmt='d'` parameter in `sns.heatmap` tells Seaborn to format the annotation numbers as integers.
>> [!success]- Answer
>> True

> [!question] What is the primary reason for setting the DataFrame index to `Date`?
> a) To sort the data alphabetically.
> b) To allow Pandas to recognize and manipulate time periods (like quarters).
> c) To remove the date column from the dataset.
> d) To make the file size smaller.
>> [!success]- Answer
>> b) To allow Pandas to recognize and manipulate time periods (like quarters).

> [!question] In the code `df.plot(kind='bar', stacked=True)`, what does the Y-axis represent?
> a) The percentage of sales.
> b) The cumulative sum of sales for all products in that month.
> c) The sales of only Product A.
> d) The average sales.
>> [!success]- Answer
>> b) The cumulative sum of sales for all products in that month.

> [!question] Which parameter in `plt.annotate` controls the style of the arrow (e.g., color, shrinkage)?
> a) `arrow_style`
> b) `pointer`
> c) `arrowprops`
> d) `xyarrow`
>> [!success]- Answer
>> c) `arrowprops`

> [!question] If `df.index.quarter` returns `1`, which months does this correspond to?
> a) January, February, March
> b) April, May, June
> c) July, August, September
> d) October, November, December
>> [!success]- Answer
>> a) January, February, March

> [!question] Why is `df.T` used specifically before creating the Heatmap?
> a) To invert the colors.
> b) To filter out NaN values.
> c) To switch Products to the Y-axis and Months to the X-axis for better readability.
> d) To sort the data by value.
>> [!success]- Answer
>> c) To switch Products to the Y-axis and Months to the X-axis for better readability.

> [!question] What does the `aggfunc='mean'` parameter do in the pivot table?
> a) It sums up all sales.
> b) It counts the number of sales.
> c) It calculates the average sales value for the group (Quarter).
> d) It finds the maximum sales value.
>> [!success]- Answer
>> c) It calculates the average sales value for the group (Quarter).

> [!question] In the visualization section, `plt.xticks(rotation=45)` is used to:
> a) Rotate the entire chart by 45 degrees.
> b) Rotate the text labels on the X-axis to prevent overlapping.
> c) Rotate the bars in the bar chart.
> d) Rotate the legend.
>> [!success]- Answer
>> b) Rotate the text labels on the X-axis to prevent overlapping.

> [!question] Which Product was identified as the "lowest performer" with the lowest variance?
> a) Product A
> b) Product B
> c) Product C
> d) Product D
>> [!success]- Answer
>> d) Product D

> [!question] The "Best Month" annotation logic used `best_month_idx + 1` for `xytext`. What does this achieve?
> a) It moves the text label slightly to the right of the bar.
> b) It moves the text label down.
> c) It corrects a calculation error.
> d) It changes the month to the next month.
>> [!success]- Answer
>> a) It moves the text label slightly to the right of the bar.

> [!question] Which library handles the `.plot()` method called directly on the DataFrame?
> a) Pure NumPy
> b) Pandas (wrapping Matplotlib)
> c) Scikit-Learn
> d) Pure Python
>> [!success]- Answer
>> b) Pandas (wrapping Matplotlib)

> [!question] Match the Plot Element to its Code.
>> [!example] Group A
>> a) `plt.legend()`
>> b) `plt.title("...")`
>> c) `plt.grid(True)`
>
>> [!example] Group B
>> n) Adds a background grid.
>> o) Adds a key identifying which color belongs to which Product.
>> p) Adds a main header text above the chart.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> p)
>> c) -> n)

> [!question] Match the Data Transformation to its Purpose.
>> [!example] Group A
>> a) `pct_change()`
>> b) `pivot_table()`
>> c) `map({...})`
>
>> [!example] Group B
>> n) Replacing raw numbers (1, 2) with labels (Q1, Q2).
>> o) summarizing data by categories (Quarters).
>> p) Calculating growth rates.
>
>> [!success]- Answer
>> a) -> p)
>> b) -> o)
>> c) -> n)

> [!question] Match the Visualization Argument to its effect.
>> [!example] Group A
>> a) `stacked=True`
>> b) `annot=True`
>> c) `aggfunc='mean'`
>
>> [!example] Group B
>> n) Displays numerical values inside Heatmap cells.
>> o) Determines how pivot table groups are summarized.
>> p) Stacks bars on top of each other in a bar chart.
>
>> [!success]- Answer
>> a) -> p)
>> b) -> n)
>> c) -> o)

> [!question] Match the Boxplot Feature to its interpretation.
>> [!example] Group A
>> a) Box position (Height)
>> b) Box size (Height of the rectangle)
>> c) Outliers (Dots outside whiskers)
>
>> [!example] Group B
>> n) Indicates volatility/spread (Interquartile Range).
>> o) Indicates extreme values.
>> p) Indicates the median sales volume.
>
>> [!success]- Answer
>> a) -> p)
>> b) -> n)
>> c) -> o)

> [!question] Match the File Operation to the code.
>> [!example] Group A
>> a) Create directory
>> b) Save CSV
>> c) Check if exists (General Context)
>
>> [!example] Group B
>> n) `df.to_csv(...)`
>> o) `os.makedirs(..., exist_ok=True)`
>> p) `os.path.exists(...)`
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)
>> c) -> p)

> [!question] Match the Axis direction to the operation result.
>> [!example] Group A
>> a) `df.sum(axis=0)` (Hypothetical)
>> b) `df.sum(axis=1)` (Used in Tutorial)
>
>> [!example] Group B
>> n) Total sales per Month (Adding Row values).
>> o) Total sales per Product (Adding Column values).
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)

> [!question] Match the Seaborn Heatmap parameter to its meaning.
>> [!example] Group A
>> a) `cmap`
>> b) `fmt`
>> c) `annot`
>
>> [!example] Group B
>> n) Toggle text labels in cells.
>> o) String format (e.g., 'd' for decimal/integer).
>> p) Colormap (color scheme).
>
>> [!success]- Answer
>> a) -> p)
>> b) -> o)
>> c) -> n)

> [!question] Match the DataFrame Index attribute to the data extracted.
>> [!example] Group A
>> a) `df.index.quarter`
>> b) `df.index.month` (General Pandas)
>> c) `df.index.year` (General Pandas)
>
>> [!example] Group B
>> n) 1 to 12
>> o) 1 to 4
>> p) 2025
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)
>> c) -> p)

> [!question] Match the Analysis Goal to the best chart.
>> [!example] Group A
>> a) Who won Q1 vs Q2?
>> b) Is Product A volatile?
>> c) Did sales spike in July?
>
>> [!example] Group B
>> n) Boxplot.
>> o) Heatmap.
>> p) Stacked Bar (grouped by Quarter).
>
>> [!success]- Answer
>> a) -> p)
>> b) -> n)
>> c) -> o)

> [!question] Match the value retrieval method.
>> [!example] Group A
>> a) `df['Total_Sales'].max()`
>> b) `df['Total_Sales'].argmax()`
>
>> [!example] Group B
>> n) The integer index position of the max value.
>> o) The actual maximum value.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)