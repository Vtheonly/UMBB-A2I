---
sources:
  - "[[04 Essential Python Libraries Part 02]]"
  - "[[09  Plotting Deep Dive (Matplotlib & Seaborn)]]"
  - "[[04 Data Visualization]]"
---
---
sources:
  - "[[04 Essential Python Libraries Part 02]]"
  - "[[09  Plotting Deep Dive (Matplotlib & Seaborn)]]"
---

> [!question] Matplotlib is built directly on top of NumPy, making it efficient for handling raw data arrays.
>> [!success]- Answer
>> True

> [!question] In the function `plt.subplots(nrows, ncols, index)`, the `index` parameter starts at 0 (zero-based indexing).
>> [!success]- Answer
>> False

> [!question] The Basemap toolkit for Matplotlib is currently the recommended library for new geospatial projects over Cartopy.
>> [!success]- Answer
>> False

> [!question] When using `plt.pie()`, the `autopct` parameter allows you to display the percentage value on the wedges.
>> [!success]- Answer
>> True

> [!question] In a Box Plot, points lying outside the "whiskers" are typically considered outliers.
>> [!success]- Answer
>> True

> [!question] The `plt.savefig()` function must be called *after* `plt.show()` to ensure the file is saved correctly.
>> [!success]- Answer
>> False

> [!question] Seaborn's `sns.heatmap()` requires a Correlation Matrix (or similar 2D grid data) as input to function correctly.
>> [!success]- Answer
>> True

> [!question] In Matplotlib, `marker='^'` renders the data points as squares.
>> [!success]- Answer
>> False

> [!question] The `**kwargs` syntax in `plt.plot` is used to unpack a dictionary of arguments (like label, linewidth) into the function.
>> [!success]- Answer
>> True

> [!question] Plotly Express (`px`) functions, such as `px.scatter`, return a static image that cannot be zoomed or panned.
>> [!success]- Answer
>> False

> [!question] Which argument is used to specify the size of the figure (width, height) in inches?
> a) `size=(w, h)`
> b) `figsize=(w, h)`
> c) `resolution=(w, h)`
> d) `dim=(w, h)`
>> [!success]- Answer
>> b) `figsize=(w, h)`

> [!question] What does `plt.tight_layout()` do?
> a) Compresses the file size of the saved image.
> b) Removes the grid lines from the plot.
> c) Automatically adjusts subplot params so that subplots and labels do not overlap.
> d) Forces all axes to have the same scale.
>> [!success]- Answer
>> c) Automatically adjusts subplot params so that subplots and labels do not overlap.

> [!question] In the format string `'b-.'`, what does the `-.` represent?
> a) A solid blue line.
> b) A dashed blue line.
> c) A dash-dot line style.
> d) A dotted line style.
>> [!success]- Answer
>> c) A dash-dot line style.

> [!question] Which Seaborn function allows you to modify the background style (e.g., adding a grid)?
> a) `sns.set_style()`
> b) `sns.change_background()`
> c) `sns.set_grid()`
> d) `sns.layout()`
>> [!success]- Answer
>> a) `sns.set_style()`

> [!question] When using `sns.heatmap`, which parameter sets the color scheme (e.g., from blue to red)?
> a) `color`
> b) `palette`
> c) `cmap`
> d) `theme`
>> [!success]- Answer
>> c) `cmap`

> [!question] Which function would you use to add a title to a specific subplot axis (`ax`) in the Object-Oriented style?
> a) `ax.title()`
> b) `ax.set_title()`
> c) `plt.title()`
> d) `ax.add_title()`
>> [!success]- Answer
>> b) `ax.set_title()`

> [!question] In a Seaborn pairplot, what is typically displayed on the diagonal axis?
> a) A scatter plot of the variable against itself.
> b) An empty space.
> c) A histogram or distribution of that single variable.
> d) A correlation number.
>> [!success]- Answer
>> c) A histogram or distribution of that single variable.

> [!question] What is the purpose of `plt.legend()`?
> a) To add a title to the plot.
> b) To label the x and y axes.
> c) To display a box explaining what each line/color represents (based on labels).
> d) To save the plot to a file.
>> [!success]- Answer
>> c) To display a box explaining what each line/color represents (based on labels).

> [!question] Which parameter in `plt.bar` controls the transparency of the bars?
> a) `transparent`
> b) `opacity`
> c) `alpha`
> d) `see_through`
>> [!success]- Answer
>> c) `alpha`

> [!question] What is the main advantage of using `plt.subplots()` over multiple calls to `plt.plot()`?
> a) It allows creating a grid of multiple separate plots in a single figure.
> b) It is faster for single plots.
> c) It automatically saves the file.
> d) It allows 3D plotting.
>> [!success]- Answer
>> a) It allows creating a grid of multiple separate plots in a single figure.

> [!question] Match the Plotly function to its specific capability.
>> [!example] Group A
>> a) px.scatter(hover_data=['...'])
>> b) fig.show()
>
>> [!example] Group B
>> n) Renders the interactive plot in the notebook.
>> o) Shows extra columns of data when the mouse moves over a point.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)

> [!question] Match the Matplotlib text function to its description.
>> [!example] Group A
>> a) plt.text(x, y, s)
>> b) plt.annotate(s, xy, xytext, arrowprops)
>
>> [!example] Group B
>> n) Adds text at an arbitrary location `x, y`.
>> o) Adds text pointing to a specific data point, often with an arrow.
>
>> [!success]- Answer
>> a) -> n)
>> b) -> o)

> [!question] Match the line style string to its visual appearance.
>> [!example] Group A
>> a) '-'
>> b) '--'
>> c) ':'
>
>> [!example] Group B
>> n) Dotted line
>> o) Solid line
>> p) Dashed line
>
>> [!success]- Answer
>> a) -> o)
>> b) -> p)
>> c) -> n)

> [!question] Match the Seaborn plot type to the kind of relationship it shows.
>> [!example] Group A
>> a) sns.lineplot
>> b) sns.boxplot
>> c) sns.heatmap
>
>> [!example] Group B
>> n) Distribution and statistical summary (quartiles).
>> o) Matrix correlation or intensity.
>> p) Trends over time or continuous sequence.
>
>> [!success]- Answer
>> a) -> p)
>> b) -> n)
>> c) -> o)

> [!question] Match the color code to the color name.
>> [!example] Group A
>> a) 'r'
>> b) 'k'
>> c) 'w'
>
>> [!example] Group B
>> n) Black
>> o) Red
>> p) White
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)
>> c) -> p)

> [!question] Match the Box Plot component to its definition.
>> [!example] Group A
>> a) Median
>> b) IQR (Interquartile Range)
>> c) Whiskers
>
>> [!example] Group B
>> n) The line extending to the rest of the distribution (usually 1.5x IQR).
>> o) The middle line inside the box (50th percentile).
>> p) The box itself, representing the middle 50% of data.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> p)
>> c) -> n)

> [!question] Match the axis label function to the interface style.
>> [!example] Group A
>> a) Pyplot Style
>> b) OO Style
>
>> [!example] Group B
>> n) `ax.set_ylabel("Label")`
>> o) `plt.ylabel("Label")`
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)

> [!question] Match the histogram parameter to its effect.
>> [!example] Group A
>> a) bins
>> b) density
>
>> [!example] Group B
>> n) Normalizes the histogram so the area sums to 1.
>> o) Determines the number of intervals (buckets) to split the data into.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)

> [!question] Match the library to its key characteristic.
>> [!example] Group A
>> a) Seaborn
>> b) Plotly
>
>> [!example] Group B
>> n) Interactive, web-based visualizations.
>> o) Statistical plotting with high-level themes, based on Matplotlib.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)

> [!question] Match the plot element to the function that creates it.
>> [!example] Group A
>> a) Title
>> b) Grid
>> c) Axis Limits
>
>> [!example] Group B
>> n) `plt.grid()`
>> o) `plt.xlim()` / `plt.ylim()`
>> p) `plt.title()`
>
>> [!success]- Answer
>> a) -> p)
>> b) -> n)
>> c) -> o)