---
sources:
  - "[[04 Essential Python Libraries Part 02]]"
  - "[[09  Plotting Deep Dive (Matplotlib & Seaborn)]]"
---
---
sources:
  - "[[04 Essential Python Libraries Part 02]]"
  - "[[09  Plotting Deep Dive (Matplotlib & Seaborn)]]"
  - "[[04 Data Visualization]]"
---

> [!question] In the function `plt.subplot(nrows, ncols, index)`, the `index` parameter allows you to select the active plot, starting at 1 (one-based indexing).
>> [!success]- Answer
>> True

> [!question] `plt.text()` differs from `plt.annotate()` because `plt.text()` is specifically designed to draw arrows pointing to data points.
>> [!success]- Answer
>> False

> [!question] Basemap is a toolkit for Matplotlib used for 2D geospatial maps, but it is deprecated in favor of Cartopy.
>> [!success]- Answer
>> True

> [!question] When using `plt.pie()`, the `labels` parameter expects a list of numeric values to determine the size of the wedges.
>> [!success]- Answer
>> False

> [!question] In Seaborn's `sns.heatmap()`, setting `annot=True` will display the actual data values (numbers) inside each cell of the matrix.
>> [!success]- Answer
>> True

> [!question] The `plt.close()` function is used to modify the axis limits of the plot.
>> [!success]- Answer
>> False

> [!question] Using `plt.tight_layout()` is recommended to prevent axis labels and titles from overlapping when working with subplots.
>> [!success]- Answer
>> True

> [!question] In the Object-Oriented interface, you call `ax.legend()` to display the legend, provided that the plotted lines have been assigned a `label`.
>> [!success]- Answer
>> True

> [!question] A Scatter plot is defined as `kind='scatter'` inside the `plt.plot()` function by default.
>> [!success]- Answer
>> False

> [!question] Plotly requires a running Jupyter Notebook environment to render interactive charts; it cannot export to static HTML.
>> [!success]- Answer
>> False

> [!question] Which Matplotlib function allows you to manually set the range of the X-axis?
> a) `plt.xrange()`
> b) `plt.xlim()`
> c) `plt.set_x()`
> d) `plt.axis_range()`
>> [!success]- Answer
>> b) `plt.xlim()`

> [!question] In the string format `'g^'`, what does the `^` symbol represent?
> a) A green line.
> b) An arrow pointing up.
> c) A triangle_up marker.
> d) A dotted line.
>> [!success]- Answer
>> c) A triangle_up marker.

> [!question] Which Seaborn parameter in `sns.heatmap` controls the formatting of the numbers displayed (e.g., to 2 decimal places)?
> a) `format`
> b) `decimals`
> c) `fmt`
> d) `style`
>> [!success]- Answer
>> c) `fmt`

> [!question] Which Plotly Express function is used to create a bar chart?
> a) `px.bar()`
> b) `px.barchart()`
> c) `plotly.bar()`
> d) `px.make_bar()`
>> [!success]- Answer
>> a) `px.bar()`

> [!question] What is the specific use of the `hue` parameter in Seaborn functions like `scatterplot`?
> a) It changes the background color.
> b) It groups data by a categorical variable and assigns different colors to each group.
> c) It increases the size of the markers based on value.
> d) It draws a regression line.
>> [!success]- Answer
>> b) It groups data by a categorical variable and assigns different colors to each group.

> [!question] Which function in Matplotlib adds a grid to the background of the plot?
> a) `plt.background(grid=True)`
> b) `plt.set_grid()`
> c) `plt.grid(True)`
> d) `plt.lines()`
>> [!success]- Answer
>> c) `plt.grid(True)`

> [!question] In the Basemap example provided, what does the `projection='merc'` argument specify?
> a) Mercator projection.
> b) Mercury projection.
> c) Meridian projection.
> d) Metric scaling.
>> [!success]- Answer
>> a) Mercator projection.

> [!question] Which function calculates the Correlation Matrix required for a heatmap?
> a) `df.heatmap()`
> b) `df.corr()`
> c) `np.correlation()`
> d) `sns.calculate_corr(df)`
>> [!success]- Answer
>> b) `df.corr()`

> [!question] What is the equivalent of `plt.xlabel("Text")` when using the Object-Oriented `ax` object?
> a) `ax.xlabel("Text")`
> b) `ax.set_label_x("Text")`
> c) `ax.set_xlabel("Text")`
> d) `ax.add_xlabel("Text")`
>> [!success]- Answer
>> c) `ax.set_xlabel("Text")`

> [!question] Why might you use `sharey=True` in `plt.subplots()`?
> a) To make all plots share the same color.
> b) To ensure all subplots use the same Y-axis scale for direct comparison.
> c) To merge all plots into one single line.
> d) To delete the Y-axis labels.
>> [!success]- Answer
>> b) To ensure all subplots use the same Y-axis scale for direct comparison.

> [!question] Match the visualization library to its primary description.
>> [!example] Group A
>> a) Matplotlib
>> b) Seaborn
>> c) Plotly
>
>> [!example] Group B
>> n) A library for creating interactive and dynamic plots.
>> o) The core Python library for plotting, offering low-level control.
>> p) A high-level library for statistical graphics, built on Matplotlib.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> p)
>> c) -> n)

> [!question] Match the heatmap parameter to its function.
>> [!example] Group A
>> a) annot
>> b) cmap
>> c) fmt
>
>> [!example] Group B
>> n) Controls the color mapping (e.g., 'coolwarm').
>> o) String formatting code (e.g., '.2f').
>> p) Boolean to write data values inside the cells.
>
>> [!success]- Answer
>> a) -> p)
>> b) -> n)
>> c) -> o)

> [!question] Match the chart type to the data relationship it visualizes.
>> [!example] Group A
>> a) Pie Chart
>> b) Line Chart
>> c) Scatter Plot
>
>> [!example] Group B
>> n) Trends over time.
>> o) Relationship between two continuous variables.
>> p) Proportions of a whole.
>
>> [!success]- Answer
>> a) -> p)
>> b) -> n)
>> c) -> o)

> [!question] Match the Seaborn aesthetic function to its purpose.
>> [!example] Group A
>> a) sns.set_style()
>> b) sns.set_context()
>
>> [!example] Group B
>> n) Scales elements (fonts, lines) for different mediums (e.g., 'talk', 'paper').
>> o) Sets the background look (e.g., 'whitegrid', 'darkgrid').
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)

> [!question] Match the Matplotlib hierarchy term to its definition.
>> [!example] Group A
>> a) Figure
>> b) Axes
>
>> [!example] Group B
>> n) The individual plot containing the data, labels, and ticks.
>> o) The top-level container (window) holding all plot elements.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)

> [!question] Match the annotation argument to its role in `plt.annotate`.
>> [!example] Group A
>> a) xy
>> b) xytext
>> c) arrowprops
>
>> [!example] Group B
>> n) The dictionary defining the style of the arrow.
>> o) The coordinates of the label text itself.
>> p) The coordinates of the point being annotated.
>
>> [!success]- Answer
>> a) -> p)
>> b) -> o)
>> c) -> n)

> [!question] Match the line style character to its visual result.
>> [!example] Group A
>> a) '-'
>> b) ':'
>
>> [!example] Group B
>> n) Solid line
>> o) Dotted line
>
>> [!success]- Answer
>> a) -> n)
>> b) -> o)

> [!question] Match the marker character to its shape.
>> [!example] Group A
>> a) 'o'
>> b) 's'
>> c) '^'
>
>> [!example] Group B
>> n) Square
>> o) Triangle
>> p) Circle
>
>> [!success]- Answer
>> a) -> p)
>> b) -> n)
>> c) -> o)

> [!question] Match the subplot parameter to its meaning in `plt.subplots`.
>> [!example] Group A
>> a) nrows
>> b) ncols
>> c) figsize
>
>> [!example] Group B
>> n) The total width and height of the figure.
>> o) The number of grid columns.
>> p) The number of grid rows.
>
>> [!success]- Answer
>> a) -> p)
>> b) -> o)
>> c) -> n)

> [!question] Match the Geospatial library to its status.
>> [!example] Group A
>> a) Basemap
>> b) Cartopy
>
>> [!example] Group B
>> n) The modern, maintained library for geospatial data.
>> o) Deprecated toolkit, but still found in legacy projects.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)