---
sources:
  - "[[04 Essential Python Libraries Part 02]]"
  - "[[09  Plotting Deep Dive (Matplotlib & Seaborn)]]"
  - "[[04 Data Visualization]]"
---

> [!question] Matplotlib offers two distinct interfaces for plotting: the Functional (Pyplot) style and the Object-Oriented (OO) style.
>> [!success]- Answer
>> True

> [!question] In the format string `'r--'`, the 'r' stands for "Round markers" and the '--' stands for "Blue line".
>> [!success]- Answer
>> False

> [!question] The function `plt.tight_layout()` is used to automatically adjust subplot parameters so that labels and titles do not overlap.
>> [!success]- Answer
>> True

> [!question] A **Histogram** is best used for comparing distinct categories (e.g., Sales per Product), whereas a **Bar Chart** is for seeing the distribution of a single numerical variable.
>> [!success]- Answer
>> False

> [!question] In the Object-Oriented approach, `ax.set_title()` is the equivalent method to `plt.title()` in the Pyplot interface.
>> [!success]- Answer
>> True

> [!question] Seaborn is a standalone library that does not depend on Matplotlib and cannot be used with Matplotlib functions.
>> [!success]- Answer
>> False

> [!question] The parameter `alpha` in plotting functions controls the transparency of the markers or lines.
>> [!success]- Answer
>> True

> [!question] `sns.pairplot()` creates a grid of scatter plots showing the relationship between every pair of numerical features in a dataset.
>> [!success]- Answer
>> True

> [!question] `plt.axvline()` is used to draw a horizontal line across the plot.
>> [!success]- Answer
>> False

> [!question] The `sharey=True` argument in `plt.subplots()` ensures that all subplots use the same scale on the Y-axis for honest comparison.
>> [!success]- Answer
>> True

> [!question] Which function creates a figure object and a set of subplots in one line?
> a) `plt.figure()`
> b) `plt.subplots()`
> c) `plt.plot()`
> d) `plt.make_grid()`
>> [!success]- Answer
>> b) `plt.subplots()`

> [!question] In Seaborn, which parameter is used to color data points based on a categorical column (adding a 3rd dimension)?
> a) `color`
> b) `palette`
> c) `style`
> d) `hue`
>> [!success]- Answer
>> d) `hue`

> [!question] What does the format string `'go'` represent in `plt.plot()`?
> a) Green circles (dots)
> b) Gold lines
> c) Grey octagons
> d) Green solid line
>> [!success]- Answer
>> a) Green circles (dots)

> [!question] Which plot type is best suited for visualizing a Correlation Matrix?
> a) Scatter Plot
> b) Histogram
> c) Heatmap
> d) Pie Chart
>> [!success]- Answer
>> c) Heatmap

> [!question] Which method saves the current figure to a file (e.g., PNG or PDF)?
> a) `plt.save()`
> b) `plt.export()`
> c) `plt.savefig()`
> d) `plt.download()`
>> [!success]- Answer
>> c) `plt.savefig()`

> [!question] In a Box Plot (or Box-and-Whisker plot), what does the central box represent?
> a) The entire range of data.
> b) The Interquartile Range (IQR) containing the middle 50% of data.
> c) The Mean and Standard Deviation.
> d) The outliers only.
>> [!success]- Answer
>> b) The Interquartile Range (IQR) containing the middle 50% of data.

> [!question] What is the specific purpose of `sns.violinplot()`?
> a) It plays music while plotting.
> b) It combines a Box Plot with a Density Plot to show data shape.
> c) It creates a 3D scatter plot.
> d) It plots time-series data only.
>> [!success]- Answer
>> b) It combines a Box Plot with a Density Plot to show data shape.

> [!question] How do you rotate the X-axis tick labels to prevent overlapping text?
> a) `plt.rotate_x(45)`
> b) `plt.xticks(rotation=45)`
> c) `plt.xlabel(angle=45)`
> d) `plt.axis(rotation='diagonal')`
>> [!success]- Answer
>> b) `plt.xticks(rotation=45)`

> [!question] Which Python library is mentioned in the notes as allowing for *interactive* and *dynamic* plots (zooming, hovering)?
> a) Matplotlib
> b) Seaborn
> c) Plotly
> d) Pandas
>> [!success]- Answer
>> c) Plotly

> [!question] In `plt.plot(x, y, label='Sales')`, what function must be called afterwards to actually display the label on the graph?
> a) `plt.show_labels()`
> b) `plt.legend()`
> c) `plt.title()`
> d) `plt.annotate()`
>> [!success]- Answer
>> b) `plt.legend()`

> [!question] Match the chart type to its "Best Use Case".
>> [!example] Group A
>> a) Line Chart
>> b) Bar Chart
>> c) Scatter Plot
>> d) Histogram
>
>> [!example] Group B
>> n) Comparing categories (discrete data).
>> o) Visualizing trends over time.
>> p) Showing the distribution/frequency of a single variable.
>> q) Detecting relationships or clusters between two numeric variables.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)
>> c) -> q)
>> d) -> p)

> [!question] Match the Pyplot command to its Object-Oriented equivalent.
>> [!example] Group A
>> a) plt.title()
>> b) plt.xlabel()
>> c) plt.legend()
>
>> [!example] Group B
>> n) ax.set_xlabel()
>> o) ax.legend()
>> p) ax.set_title()
>
>> [!success]- Answer
>> a) -> p)
>> b) -> n)
>> c) -> o)

> [!question] Match the Matplotlib format string symbol to its visual result.
>> [!example] Group A
>> a) '--'
>> b) ':'
>> c) 'o'
>> d) '^'
>
>> [!example] Group B
>> n) Dotted line
>> o) Dashed line
>> p) Triangle marker
>> q) Circle marker
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)
>> c) -> q)
>> d) -> p)

> [!question] Match the annotation function to its behavior.
>> [!example] Group A
>> a) plt.text()
>> b) plt.annotate()
>
>> [!example] Group B
>> n) Adds text at an exact x,y coordinate with no arrow.
>> o) Adds text with an optional arrow pointing to a specific data point.
>
>> [!success]- Answer
>> a) -> n)
>> b) -> o)

> [!question] Match the Seaborn function to the type of plot it generates.
>> [!example] Group A
>> a) sns.heatmap()
>> b) sns.barplot()
>> c) sns.scatterplot()
>
>> [!example] Group B
>> n) Relational Plot
>> o) Matrix Plot
>> p) Categorical Plot
>
>> [!success]- Answer
>> a) -> o)
>> b) -> p)
>> c) -> n)

> [!question] Match the hierarchy component to its definition.
>> [!example] Group A
>> a) Figure
>> b) Axes
>> c) Axis
>
>> [!example] Group B
>> n) The actual plotting area (where the graph is drawn).
>> o) The overall window or page containing everything.
>> p) The number-line (x-axis or y-axis) that sets the scale.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)
>> c) -> p)

> [!question] Match the parameter to its function in `plt.plot`.
>> [!example] Group A
>> a) linewidth
>> b) markersize
>> c) color
>
>> [!example] Group B
>> n) Sets the thickness of the line.
>> o) Sets the size of the dots/symbols.
>> p) Sets the color of the line/marker.
>
>> [!success]- Answer
>> a) -> n)
>> b) -> o)
>> c) -> p)

> [!question] Match the Seaborn style function to its purpose.
>> [!example] Group A
>> a) sns.set_theme()
>> b) sns.set_context()
>
>> [!example] Group B
>> n) Modifies text sizes and elements (e.g., 'talk', 'paper').
>> o) Sets the overall visual theme (e.g., 'whitegrid').
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)

> [!question] Match the hel