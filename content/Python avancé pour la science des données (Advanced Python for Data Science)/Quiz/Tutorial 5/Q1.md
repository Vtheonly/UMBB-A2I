---
sources:
  - "[[Tutorial 05 Medical Data Cleaning & Visualization]]"
---

> [!question] In the tutorial, the string "None" (as text) is automatically treated as a missing value (NaN) by Pandas without any extra code.
>> [!success]- Answer
>> False

> [!question] The `MinMaxScaler` is used to convert categorical text labels (like "Diabetes") into integers (like 0 or 1).
>> [!success]- Answer
>> False

> [!question] We filled missing numerical values (Age, Blood Pressure) with the **Median** because the Mean is too sensitive to outliers.
>> [!success]- Answer
>> True

> [!question] The method `df['Gender'].str.lower().str.strip()` helps resolve issues where 'Female', 'female', and ' female ' are treated as different categories.
>> [!success]- Answer
>> True

> [!question] In a Box Plot, the box represents the entire range of the data from the minimum value to the maximum value.
>> [!success]- Answer
>> False

> [!question] Label Encoding is preferred over One-Hot Encoding when we want to transform a text column into a single column of numbers (e.g., 0, 1, 2).
>> [!success]- Answer
>> True

> [!question] The code `df = df[df['Age'] <= 100]` removes rows where the patient is older than 100 years.
>> [!success]- Answer
>> True

> [!question] A Heatmap is an effective way to visualize the individual data points of every single patient in a scatter format.
>> [!success]- Answer
>> False

> [!question] When filling missing categorical data (like Gender), the tutorial uses the **Mode** (the most frequent value).
>> [!success]- Answer
>> True

> [!question] `sns.set_theme()` is a mandatory command; without it, Matplotlib plots will fail to render.
>> [!success]- Answer
>> False

> [!question] Why did we use `replace('none', np.nan, inplace=True)`?
> a) To replace the text string "none" with a true Pandas null value.
> b) To delete the rows containing "none".
> c) To replace the text "none" with the number 0.
> d) To fix a spelling error.
>> [!success]- Answer
>> a) To replace the text string "none" with a true Pandas null value.

> [!question] Which library class was used to scale the Age and Blood Pressure to a range of 0-1?
> a) `StandardScaler`
> b) `MinMaxScaler`
> c) `RobustScaler`
> d) `Normalizer`
>> [!success]- Answer
>> b) `MinMaxScaler`

> [!question] In the scatter plot code `sns.scatterplot(..., hue='Gender')`, what does the `hue` parameter do?
> a) It changes the size of the dots based on Gender.
> b) It colors the dots differently based on Gender.
> c) It filters the data to show only one Gender.
> d) It changes the shape of the markers.
>> [!success]- Answer
>> b) It colors the dots differently based on Gender.

> [!question] Why is it necessary to scale features like Age (0-100) and Blood Pressure (100-200) for certain Machine Learning algorithms?
> a) To save memory.
> b) To make the graph look prettier.
> c) To ensure features with larger numbers don't dominate features with smaller numbers.
> d) To remove missing values.
>> [!success]- Answer
>> c) To ensure features with larger numbers don't dominate features with smaller numbers.

> [!question] What does `df['Gender'].mode()[0]` return?
> a) The count of the most common gender.
> b) The actual string value of the most common gender (e.g., 'female').
> c) A list of all unique genders.
> d) The number of missing values.
>> [!success]- Answer
>> b) The actual string value of the most common gender (e.g., 'female').

> [!question] What issue does the patient with Age 120 represent?
> a) A Missing Value.
> b) A Formatting Error.
> c) An Outlier / Data Entry Error.
> d) A Duplicate.
>> [!success]- Answer
>> c) An Outlier / Data Entry Error.

> [!question] Which visualization was used to compare the **average** Blood Pressure across different Genders and Diagnoses in a grid format?
> a) Box Plot
> b) Heatmap
> c) Scatter Plot
> d) Histogram
>> [!success]- Answer
>> b) Heatmap

> [!question] What does `LabelEncoder` do to the 'Diagnosis' column?
> a) Removes the column.
> b) Converts 'Healthy'/'Diabetes' into numbers like 0 and 1.
> c) One-hot encodes the column into multiple columns.
> d) Fills missing values.
>> [!success]- Answer
>> b) Converts 'Healthy'/'Diabetes' into numbers like 0 and 1.

> [!question] How is the `pivot_table` used in this tutorial?
> a) To prepare data for the Heatmap.
> b) To remove outliers.
> c) To calculate the median age.
> d) To load the CSV file.
>> [!success]- Answer
>> a) To prepare data for the Heatmap.

> [!question] Which Pandas method was used to fix inconsistent capitalization (e.g., 'Female' vs 'female')?
> a) `df.upper()`
> b) `df.capitalize()`
> c) `df.str.lower()`
> d) `df.clean_text()`
>> [!success]- Answer
>> c) `df.str.lower()`

> [!question] Match the Preprocessing Step to its Purpose.
>> [!example] Group A
>> a) Imputation
>> b) Scaling
>> c) Encoding
>> d) Standardization
>
>> [!example] Group B
>> n) Fixing inconsistent string formatting (lowercase/strip).
>> o) Filling missing values (NaNs).
>> p) Converting text categories into numbers.
>> q) Adjusting numerical ranges to be comparable (0 to 1).
>
>> [!success]- Answer
>> a) -> o)
>> b) -> q)
>> c) -> p)
>> d) -> n)

> [!question] Match the Cleaning Strategy to the Data Type.
>> [!example] Group A
>> a) Categorical Missing Data (Gender)
>> b) Numerical Missing Data (Age)
>> c) Numerical Outliers (Age > 100)
>
>> [!example] Group B
>> n) Fill with Mode.
>> o) Remove or Cap.
>> p) Fill with Median.
>
>> [!success]- Answer
>> a) -> n)
>> b) -> p)
>> c) -> o)

> [!question] Match the Visualization to the Insight.
>> [!example] Group A
>> a) Scatter Plot
>> b) Box Plot
>> c) Heatmap
>
>> [!example] Group B
>> n) Shows correlation between Age and Blood Pressure.
>> o) Shows that Males with Hypertension have the highest average BP.
>> p) Shows distribution of Cholesterol ranges per Diagnosis.
>
>> [!success]- Answer
>> a) -> n)
>> b) -> p)
>> c) -> o)

> [!question] Match the Library to the Class/Function used.
>> [!example] Group A
>> a) sklearn.preprocessing
>> b) seaborn
>> c) pandas
>
>> [!example] Group B
>> n) `read_csv`, `fillna`
>> o) `MinMaxScaler`, `LabelEncoder`
>> p) `scatterplot`, `heatmap`
>
>> [!success]- Answer
>