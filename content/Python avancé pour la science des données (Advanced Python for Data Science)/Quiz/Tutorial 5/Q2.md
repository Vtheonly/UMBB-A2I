---
sources:
  - "[[Tutorial 05 Medical Data Cleaning & Visualization]]"
---

> [!question] The method `fit_transform()` performs two actions: it calculates the necessary parameters (like min/max) and then applies the transformation to the data.
>> [!success]- Answer
>> True

> [!question] Dropping rows with missing values (`dropna`) is the strategy used in this tutorial to handle the missing Age and Cholesterol data.
>> [!success]- Answer
>> False

> [!question] The code `df['Gender'].astype(str)` is used to ensure all entries in the column are treated as strings before applying string methods.
>> [!success]- Answer
>> True

> [!question] `MinMaxScaler` changes the shape of the data distribution (e.g., turns a flat distribution into a bell curve).
>> [!success]- Answer
>> False

> [!question] In the tutorial, the Heatmap is generated directly from the raw DataFrame without any aggregation or pivoting.
>> [!success]- Answer
>> False

> [!question] `sns.scatterplot` allows mapping a third variable (like Gender) to the color of the points using the `hue` parameter.
>> [!success]- Answer
>> True

> [!question] The outlier removal step (`Age <= 100`) is performed *after* the Scaling and Encoding steps in the provided notebook.
>> [!success]- Answer
>> False

> [!question] K-Nearest Neighbors is mentioned as an algorithm that behaves poorly if variables have vastly different scales.
>> [!success]- Answer
>> True

> [!question] The `mode()` function returns a single scalar value directly, so `[0]` indexing is not necessary.
>> [!success]- Answer
>> False

> [!question] `df.pivot_table` with `aggfunc='mean'` calculates the average value of the target variable for each group combination.
>> [!success]- Answer
>> True

> [!question] What is the specific purpose of the `inplace=True` parameter in `df.fillna()`?
> a) It creates a copy of the dataframe with filled values.
> b) It fills values in the existing dataframe without needing reassignment.
> c) It fills values only in numerical columns.
> d) It validates the data types.
>> [!success]- Answer
>> b) It fills values in the existing dataframe without needing reassignment.

> [!question] Why is `LabelEncoder` used on the 'Diagnosis' column?
> a) To fix spelling mistakes.
> b) To normalize the text to lowercase.
> c) To transform text labels (Healthy, Diabetes) into machine-readable integers.
> d) To visualize the distribution.
>> [!success]- Answer
>> c) To transform text labels (Healthy, Diabetes) into machine-readable integers.

> [!question] In the boxplot `sns.boxplot(x=df['Age'])`, what does the central line inside the box represent?
> a) The Mean.
> b) The Median.
> c) The Mode.
> d) The Outlier threshold.
>> [!success]- Answer
>> b) The Median.

> [!question] Which Matplotlib/Seaborn colormap was used for the Heatmap in the notebook?
> a) `viridis`
> b) `plasma`
> c) `coolwarm`
> d) `grayscale`
>> [!success]- Answer
>> c) `coolwarm`

> [!question] What does `le.classes_` contain after fitting a LabelEncoder?
> a) The encoded integer values.
> b) The unique original text labels that were encoded.
> c) The number of missing values.
> d) The data types of the columns.
>> [!success]- Answer
>> b) The unique original text labels that were encoded.

> [!question] If we used the **Mean** instead of the Median to fill missing Ages, what would happen due to the outlier (Age 120)?
> a) The filled value would be artificially lower.
> b) The filled value would be pulled higher (skewed).
> c) The code would crash.
> d) Nothing changes, Mean and Median are the same.
>> [!success]- Answer
>> b) The filled value would be pulled higher (skewed).

> [!question] Which step transforms the `Gender` column from `['Female', 'male', 'Male']` to `['female', 'male', 'male']`?
> a) Imputation
> b) Standardization / String Cleaning
> c) One-Hot Encoding
> d) Scaling
>> [!success]- Answer
>> b) Standardization / String Cleaning

> [!question] What does `df.isnull().sum()` tell us about the 'Gender' column after Imputation?
> a) It should return 0.
> b) It should return the total number of rows.
> c) It should return the number of 'male' entries.
> d) It should return the count of outliers.
>> [!success]- Answer
>> a) It should return 0.

> [!question] In the visualization section, `s=100` in `sns.scatterplot` controls:
> a) The shape of the markers.
> b) The size of the markers.
> c) The transparency of the markers.
> d) The sorting order.
>> [!success]- Answer
>> b) The size of the markers.

> [!question] How is the raw dataset created in the notebook?
> a) It downloads a file from the internet.
> b) It reads a pre-existing Excel file.
> c) It defines a Python dictionary and converts it to a DataFrame.
> d) It uses a random number generator for all columns.
>> [!success]- Answer
>> c) It defines a Python dictionary and converts it to a DataFrame.

> [!question] Match the Scikit-Learn Class to its function.
>> [!example] Group A
>> a) LabelEncoder
>> b) MinMaxScaler
>> c) StandardScaler (General Knowledge/Context)
>
>> [!example] Group B
>> n) Scales data to a fixed range [0, 1].
>> o) Encodes target labels with value between 0 and n_classes-1.
>> p) Scales data to mean=0 and std=1.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)
>> c) -> p)

> [!question] Match the Pandas/Python operation to the Cleaning Step.
>> [!example] Group A
>> a) `str.strip()`
>> b) `fillna()`
>> c) `df[df['Age'] <= 100]`
>
>> [!example] Group B
>> n) Outlier Removal.
>> o) String Standardization.
>> p) Imputation.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> p)
>> c) -> n)

> [!question] Match the visualization to the variables analyzed in the tutorial.
>> [!example] Group A
>> a) Scatter Plot
>> b) Box Plot
>> c) Heatmap
>
>> [!example] Group B
>> n) Age vs Blood Pressure.
>> o) Diagnosis vs Cholesterol.
>> p) Diagnosis vs Gender (Average BP).
>
>> [!success]- Answer
>> a) -> n)
>> b) -> o)
>> c) -> p)

> [!question] Match the missing value strategy to the data type.
>> [!example] Group A
>> a) Numerical (Skewed/Outliers present)
>> b) Categorical
>
>> [!example] Group B
>> n) Mode (Most Frequent).
>> o) Median.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)

> [!question] Match the output of `df.describe()` (not shown in this specific notebook, but implied context) vs `df['Col'].unique()`.
>> [!example] Group A
>> a) `unique()`
>> b) `describe()`
>
>> [!example] Group B
>> n) Shows all distinct values (useful for checking categories).
>> o) Shows statistical summary (mean, std, min, max).
>
>> [!success]- Answer
>> a) -> n)
>> b) -> o)

> [!question] Match the plotting parameter to its effect.
>> [!example] Group A
>> a) `hue='Gender'`
>> b) `style='Gender'`
>> c) `linewidths=1`
>
>> [!example] Group B
>> n) Changes marker symbols (circle, x) based on category.
>> o) Adds separation lines between cells in a heatmap.
>> p) Colors points differently based on category.
>
>> [!success]- Answer
>> a) -> p)
>> b) -> n)
>> c) -> o)

> [!question] Match the variable name to the result of the transformation.
>> [!example] Group A
>> a) `Age_Scaled`
>> b) `Gender_Code`
>> c) `Diagnosis_Code`
>
>> [!example] Group B
>> n) Values like 0 or 1 (from LabelEncoder).
>> o) Values between 0.0 and 1.0.
>> p) Values like 0 or 1 (from LabelEncoder).
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)
>> c) -> p)

> [!question] Match the Data Issue to the solution.
>> [!example] Group A
>> a) "MALE" vs "Male"
>> b) "none" string
>> c) Age 120
>
>> [!example] Group B
>> n) `replace('none', np.nan)`
>> o) `str.lower()`
>> p) Filter row logic.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)
>> c) -> p)

> [!question] Match the Seaborn style setting to its effect.
>> [!example] Group A
>> a) `style="whitegrid"`
>> b) `palette="pastel"`
>
>> [!example] Group B
>> n) Sets the color scheme for the charts.
>> o) Sets the background to white with gridlines.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)

> [!question] Match the Pivot Table argument to its role.
>> [!example] Group A
>> a) `index`
>> b) `columns`
>> c) `values`
>
>> [!example] Group B
>> n) The data we want to average (BloodPressure).
>> o) The rows of the new table (Diagnosis).
>> p) The columns of the new table (Gender).
>
>> [!success]- Answer
>> a) -> o)
>> b) -> p)
>> c) -> n)