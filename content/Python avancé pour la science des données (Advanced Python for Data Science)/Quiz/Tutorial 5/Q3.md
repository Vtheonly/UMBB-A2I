---
sources:
  - "[[Tutorial 05 Medical Data Cleaning & Visualization]]"
---

> [!question] The `LabelEncoder` class modifies the dataframe columns in place (i.e., overwrites the text data) automatically when you call `fit_transform`.
>> [!success]- Answer
>> False

> [!question] When saving the generated messy dataset, `index=False` is used in `to_csv` to prevent writing the row numbers into the file.
>> [!success]- Answer
>> True

> [!question] The presence of an outlier like Age 120 significantly shifts the Mean value, but has little to no effect on the Median value.
>> [!success]- Answer
>> True

> [!question] In the Heatmap visualization code `fmt=".1f"`, the format specifier tells Seaborn to display the numbers with one decimal place.
>> [!success]- Answer
>> True

> [!question] The code `df['Age'] <= 100` returns a new DataFrame containing only the valid rows.
>> [!success]- Answer
>> False

> [!question] `np.nan` (Not a Number) is technically considered a float data type in Python/Pandas.
>> [!success]- Answer
>> True

> [!question] The `pivot_table` function in the notebook aggregates data by summing (`sum`) the blood pressure values.
>> [!success]- Answer
>> False

> [!question] We performed Scaling *before* Outlier Removal in the tutorial pipeline.
>> [!success]- Answer
>> False

> [!question] `LabelEncoder` transforms categorical labels into binary vectors (e.g., [0, 1] and [1, 0]).
>> [!success]- Answer
>> False

> [!question] The `style` parameter in `sns.scatterplot` changes the shape of the markers (e.g., circles vs crosses) based on a categorical variable.
>> [!success]- Answer
>> True

> [!question] What is the return type of the function call `df['Gender'].mode()`?
> a) A string.
> b) An integer.
> c) A Pandas Series.
> d) A NumPy array.
>> [!success]- Answer
>> c) A Pandas Series.

> [!question] In the `MinMaxScaler` transformation, what mathematical value does the minimum value of the original column become?
> a) -1
> b) 0
> c) 0.5
> d) It remains unchanged.
>> [!success]- Answer
>> b) 0

> [!question] Which library is used to define the `np.nan` value used for replacement?
> a) Pandas
> b) Scikit-Learn
> c) NumPy
> d) Matplotlib
>> [!success]- Answer
>> c) NumPy

> [!question] If we did not convert the 'Gender' column to string using `.astype(str)` before string manipulation, what would likely happen if the column contained mixed types?
> a) It would work perfectly.
> b) It would raise an AttributeError or treat non-strings as NaNs effectively.
> c) It would automatically convert everything to integers.
> d) It would delete the column.
>> [!success]- Answer
>> b) It would raise an AttributeError or treat non-strings as NaNs effectively.

> [!question] What is the visual purpose of `cmap="coolwarm"` in the Heatmap?
> a) To make the text bold.
> b) To map low values to cool colors (blue) and high values to warm colors (red).
> c) To remove the grid lines.
> d) To sort the axes alphabetically.
>> [!success]- Answer
>> b) To map low values to cool colors (blue) and high values to warm colors (red).

> [!question] Which visualization was specifically used to check the distribution and potential skewness of the 'Age' column *before* cleaning?
> a) Heatmap
> b) Scatter Plot
> c) Box Plot
> d) Bar Chart
>> [!success]- Answer
>> c) Box Plot

> [!question] Why is the `PatientID` column generally excluded from analysis steps like correlation or scaling?
> a) It is an identifier with no statistical relationship to health.
> b) It contains too many missing values.
> c) It is a boolean value.
> d) It is too large to process.
>> [!success]- Answer
>> a) It is an identifier with no statistical relationship to health.

> [!question] In the code `df.pivot_table(index='Diagnosis', columns='Gender', ...)`, what defines the rows of the resulting table?
> a) Gender
> b) BloodPressure
> c) Diagnosis
> d) PatientID
>> [!success]- Answer
>> c) Diagnosis

> [!question] What does `display(df.head())` do in a Jupyter Notebook?
> a) Prints the last 5 rows.
> b) Renders the first 5 rows in a formatted HTML table.
> c) Prints the column data types.
> d) Shows a summary of missing values.
>> [!success]- Answer
>> b) Renders the first 5 rows in a formatted HTML table.

> [!question] If `LabelEncoder` assigns 'Healthy' -> 0 and 'Diabetes' -> 1, what does this imply for some machine learning models?
> a) Nothing, they are treated equally.
> b) That 'Diabetes' is "greater than" 'Healthy', which implies an order.
> c) That they are separate binary columns.
> d) That 'Healthy' is a missing value.
>> [!success]- Answer
>> b) That 'Diabetes' is "greater than" 'Healthy', which implies an order.

> [!question] Match the Transformation Method to the Column it was applied to.
>> [!example] Group A
>> a) `str.lower()`
>> b) `MinMaxScaler()`
>> c) `LabelEncoder()`
>
>> [!example] Group B
>> n) Age
>> o) Diagnosis
>> p) Gender
>
>> [!success]- Answer
>> a) -> p)
>> b) -> n)
>> c) -> o)

> [!question] Match the Visualization Parameter to its description.
>> [!example] Group A
>> a) `s=100`
>> b) `fmt=".1f"`
>> c) `linewidths=1`
>
>> [!example] Group B
>> n) Controls the decimal precision of text in a heatmap.
>> o) Controls the thickness of lines separating heatmap cells.
>> p) Controls the size of points in a scatterplot.
>
>> [!success]- Answer
>> a) -> p)
>> b) -> n)
>> c) -> o)

> [!question] Match the Raw Data Value to the issue it represents.
>> [!example] Group A
>> a) `np.nan`
>> b) `120`
>> c) `female` vs `Female`
>
>> [!example] Group B
>> n) Inconsistent string formatting.
>> o) Missing Value.
>> p) Outlier.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> p)
>> c) -> n)

> [!question] Match the Code Object to its Type.
>> [!example] Group A
>> a) `df`
>> b) `le`
>> c) `scaler`
>
>> [!example] Group B
>> n) `MinMaxScaler` instance.
>> o) `LabelEncoder` instance.
>> p) `pandas.DataFrame`.
>
>> [!success]- Answer
>> a) -> p)
>> b) -> o)
>> c) -> n)

> [!question] Match the Cleaning Action to the logic used.
>> [!example] Group A
>> a) Filling Numerical Data
>> b) Filling Categorical Data
>> c) Standardizing Text
>
>> [!example] Group B
>> n) Use Mode (Most Frequent).
>> o) Use `str.strip()` and `str.lower()`.
>> p) Use Median (Robust to outliers).
>
>> [!success]- Answer
>> a) -> p)
>> b) -> n)
>> c) -> o)

> [!question] Match the Seaborn plot function to the number of variables (dimensions) visualized in the tutorial.
>> [!example] Group A
>> a) `boxplot(x=Age)`
>> b) `scatterplot(x=Age, y=BP, hue=Gender)`
>
>> [!example] Group B
>> n) 3 Variables (2 Numerical + 1 Categorical).
>> o) 1 Variable (Numerical distribution).
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)

> [!question] Match the DataFrame attribute to its purpose.
>> [!example] Group A
>> a) `df.columns`
>> b) `df.shape`
>> c) `df.dtypes`
>
>> [!example] Group B
>> n) Shows the dimensions (rows, cols).
>> o) Shows the data type of each column (int, float, object).
>> p) Shows the names of the columns.
>
>> [!success]- Answer
>> a) -> p)
>> b) -> n)
>> c) -> o)

> [!question] Match the Pandas selection syntax.
>> [!example] Group A
>> a) `df['Age']`
>> b) `df[['Age', 'BP']]`
>> c) `df[df['Age'] > 50]`
>
>> [!example] Group B
>> n) Returns a filtered DataFrame based on a condition.
>> o) Returns a Series (Single column).
>> p) Returns a DataFrame with selected columns.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> p)
>> c) -> n)

> [!question] Match the Library to the Visualization Type provided.
>> [!example] Group A
>> a) Seaborn
>> b) Matplotlib.pyplot (used for setup)
>
>> [!example] Group B
>> n) `figure()`, `title()`, `show()`.
>> o) `heatmap()`, `boxplot()`, `scatterplot()`.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)

> [!question] Match the Categorical Encoding result.
>> [!example] Group A
>> a) Healthy
>> b) Diabetes
>
>> [!example] Group B
>> n) Encoded as 1 (Assuming alphabetical order D < H, wait... D comes before H).
>> o) Encoded as 0 (Assuming alphabetical order D comes before H).
>
>> [!success]- Answer
>> a) -> n)
>> b) -> o)