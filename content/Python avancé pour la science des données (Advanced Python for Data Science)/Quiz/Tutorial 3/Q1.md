---
sources:
  - "[[Tutorial 03 Python Programming Essentials]]"
---

> [!question] The file `__init__.py` must contain initialization code for the `utils` folder to be recognized as a Python package.
>> [!success]- Answer
>> False

> [!question] Using `f.readlines()` is the most memory-efficient way to process a file containing 1 million lines.
>> [!success]- Answer
>> False

> [!question] In the `Product` class, the `__repr__` method is responsible for calculating the total value of the product.
>> [!success]- Answer
>> False

> [!question] The `try...except ValueError` block in the `parse_products` function prevents the program from crashing if a line contains "BadData".
>> [!success]- Answer
>> True

> [!question] A function becomes a **Generator** automatically if it contains the `yield` keyword instead of `return`.
>> [!success]- Answer
>> True

> [!question] The method `.strip()` removes whitespace from the beginning and end of a string, but leaves internal spaces (like in "Hard Drive") alone.
>> [!success]- Answer
>> True

> [!question] In the modular structure, the `scripts/` folder is intended to hold the reusable library code and helper functions.
>> [!success]- Answer
>> False

> [!question] The code `sys.path.append('..')` is used to add the parent directory to the system path, allowing imports from the `utils` folder.
>> [!success]- Answer
>> True

> [!question] Object-Oriented Programming (OOP) allows us to encapsulate data (attributes) and logic (methods) into a single entity called a Class.
>> [!success]- Answer
>> True

> [!question] In the notebook, the `clean_lines` function converts the price string into a float.
>> [!success]- Answer
>> False

> [!question] What is the primary purpose of the `__init__.py` file inside the `utils/` folder?
> a) It initializes the Git repository.
> b) It tells Python to treat the directory as an importable package.
> c) It contains the main execution script.
> d) It stores the raw data variables.
>> [!success]- Answer
>> b) It tells Python to treat the directory as an importable package.

> [!question] Which method acts as the "Constructor" in a Python class, initializing the object's attributes?
> a) `__repr__`
> b) `__str__`
> c) `__init__`
> d) `__new__`
>> [!success]- Answer
>> c) `__init__`

> [!question] Why is a Generator (`yield`) preferred over a List for "Big Data"?
> a) Generators process data faster.
> b) Generators allow random access to elements.
> c) Generators sort the data automatically.
> d) Generators process one item at a time, keeping RAM usage low.
>> [!success]- Answer
>> d) Generators process one item at a time, keeping RAM usage low.

> [!question] In the code `Product(name='Laptop', total=3000.0)`, which magic method controls this string output?
> a) `__init__`
> b) `__repr__`
> c) `__print__`
> d) `__call__`
>> [!success]- Answer
>> b) `__repr__`

> [!question] What does the `parse_products` function return in the tutorial?
> a) A list of Product objects.
> b) A list of strings.
> c) A list of tuples containing (name, price, quantity).
> d) A pandas DataFrame.
>> [!success]- Answer
>> c) A list of tuples containing (name, price, quantity).

> [!question] How does the script handle a line like `"BadData,Free,Five"`?
> a) It crashes with a ValueError.
> b) It converts "Free" to 0.0.
> c) It catches the error, prints a message, and skips the line.
> d) It deletes the file.
>> [!success]- Answer
>> c) It catches the error, prints a message, and skips the line.

> [!question] What is the result of `line.strip()` if `line = "  Laptop  \n"`?
> a) `"  Laptop  "`
> b) `"Laptop\n"`
> c) `"Laptop"`
> d) `"Laptop  "`
>> [!success]- Answer
>> c) `"Laptop"`

> [!question] In the `Product` class, what does `self` represent?
> a) The class blueprint itself.
> b) The global variable scope.
> c) The specific instance of the object being manipulated.
> d) The parent directory.
>> [!success]- Answer
>> c) The specific instance of the object being manipulated.

> [!question] What is the purpose of `os.makedirs('../data/raw', exist_ok=True)`?
> a) To delete the folder if it exists.
> b) To create the folder only if it doesn't already exist (avoiding errors).
> c) To rename the folder.
> d) To list files in the folder.
>> [!success]- Answer
>> b) To create the folder only if it doesn't already exist (avoiding errors).

> [!question] Which Python concept is used to model business entities like "Product" in this tutorial?
> a) Functional Programming
> b) Object-Oriented Programming (OOP)
> c) Procedural Programming
> d) Metaprogramming
>> [!success]- Answer
>> b) Object-Oriented Programming (OOP)

> [!question] Match the file reading method to its behavior.
>> [!example] Group A
>> a) f.readlines()
>> b) for line in f: ... yield line
>
>> [!example] Group B
>> n) Lazy Evaluation (One line at a time).
>> o) Eager Evaluation (Loads entire file to RAM).
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)

> [!question] Match the Project Directory to its content.
>> [!example] Group A
>> a) utils/
>> b) data/raw/
>> c) scripts/
>> d) notebooks/
>
>> [!example] Group B
>> n) Input text files.
>> o) Reusable helper modules.
>> p) Analysis and exploration files.
>> q) Main execution files (entry points).
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)
>> c) -> q)
>> d) -> p)

> [!question] Match the Python Class method to its role.
>> [!example] Group A
>> a) __init__
>> b) total_value
>> c) __repr__
>
>> [!example] Group B
>> n) Calculates Price * Quantity.
>> o) Provides a string representation for debugging.
>> p) Initializes the object's attributes.
>
>> [!success]- Answer
>> a) -> p)
>> b) -> n)
>> c) -> o)

> [!question] Match the Code Snippet to its function.
>> [!example] Group A
>> a) line.strip()
>> b) line.split(',')
>> c) float(parts[1])
>
>> [!example] Group B
>> n) Converts a string to a decimal number.
>> o) Breaks a string into a list based on a delimiter.
>> p) Removes leading/trailing whitespace.
>
>> [!success]- Answer
>> a) -> p)
>> b) -> o)
>> c) -> n)

> [!question] Match the error handling term to its definition.
>> [!example] Group A
>> a) try
>> b) except
>> c) ValueError
>
>> [!example] Group B
>> n) The specific error type (e.g., converting text to int).
>> o) The block of code to execute if an error occurs.
>> p) The block of code to test for errors.
>
>> [!success]- Answer
>> a) -> p)
>> b) -> o)
>> c) -> n)

> [!question] Match the variable to what it stores in the `parse_products` function.
>> [!example] Group A
>> a) parts[0]
>> b) parts[1]
>> c) parts[2]
>
>> [!example] Group B
>> n) Quantity (int)
>> o) Name (string)
>> p) Price (float)
>
>> [!success]- Answer
>> a) -> o)
>> b) -> p)
>> c) -> n)

> [!question] Match the Matplotlib element to the code used to create it.
>> [!example] Group A
>> a) The Bars
>> b) The Grid
>> c) The Text Labels
>
>> [!example] Group B
>> n) plt.grid(axis='y')
>> o) plt.text()
>> p) plt.bar()
>
>> [!success]- Answer
>> a) -> p)
>> b) -> n)
>> c) -> o)

> [!question] Match the concept to the analogy provided in the tutorial.
>> [!example] Group A
>> a) readlines()
>> b) Generator
>
>> [!example] Group B
>> n) A Conveyor Belt (process one, move to next).
>> o) Dumping the entire warehouse truck onto the floor.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)

> [!question] Match the Module to the file it is defined in.
>> [!example] Group A
>> a) clean_lines
>> b) Product
>
>> [!example] Group B
>> n) utils/product.py
>> o) utils/helpers.py
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)

> [!question] Match the standard library module to its usage.
>> [!example] Group A
>> a) sys
>> b) os
>> c) matplotlib.pyplot
>
>> [!example] Group B
>> n) Creating directories.
>> o) Modifying the import path.
>> p) Plotting the sales chart.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)
>> c) -> p)