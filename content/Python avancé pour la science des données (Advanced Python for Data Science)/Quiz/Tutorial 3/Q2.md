---
sources:
  - "[[Tutorial 03 Python Programming Essentials]]"
---

> [!question] The `sys.path.append('..')` command is used to remove the parent directory from the Python search path.
>> [!success]- Answer
>> False

> [!question] In the `clean_lines` function, `line.strip()` is used to remove the newline character (`\n`) and surrounding spaces from each line.
>> [!success]- Answer
>> True

> [!question] A Python generator calculates all values immediately and stores them in a list before returning.
>> [!success]- Answer
>> False

> [!question] The `Product` class calculates `total_value()` by adding the price and quantity together.
>> [!success]- Answer
>> False

> [!question] In Matplotlib, `plt.bar()` creates a vertical bar chart, whereas `plt.plot()` typically creates a line chart.
>> [!success]- Answer
>> True

> [!question] The `if not stripped: continue` line in the parser function is used to stop the loop completely if an empty line is found.
>> [!success]- Answer
>> False

> [!question] The `__repr__` method must return a string.
>> [!success]- Answer
>> True

> [!question] If `int(parts[2])` encounters the string "Five", it will raise a `ValueError`.
>> [!success]- Answer
>> True

> [!question] To define a generator function, you must use the keyword `return` inside the loop.
>> [!success]- Answer
>> False

> [!question] The `utils` folder requires an `__init__.py` file (even if empty) to be recognized as a package by Python.
>> [!success]- Answer
>> True

> [!question] What is the specific purpose of `ha='center'` and `va='bottom'` in the `plt.text()` function?
> a) It sets the font size and color.
> b) It aligns the text relative to the coordinates provided.
> c) It rotates the text 90 degrees.
> d) It places the text in the exact center of the screen.
>> [!success]- Answer
>> b) It aligns the text relative to the coordinates provided.

> [!question] Why does the `parse_products` function check `if len(parts) != 3:`?
> a) To ensure the line has exactly Name, Price, and Quantity.
> b) To ensure the line has less than 3 characters.
> c) To check if the file is empty.
> d) To verify the file extension.
>> [!success]- Answer
>> a) To ensure the line has exactly Name, Price, and Quantity.

> [!question] What happens if you try to run `float("Free")` inside the `try` block?
> a) The program stops immediately.
> b) The value becomes 0.0.
> c) The code jumps to the `except ValueError` block.
> d) The code jumps to the `finally` block.
>> [!success]- Answer
>> c) The code jumps to the `except ValueError` block.

> [!question] Which statement best describes the "Lazy Evaluation" of a generator?
> a) It ignores errors to save time.
> b) It calculates values only when they are requested.
> c) It runs slowly because it uses Python instead of C.
> d) It loads all data into RAM but processes it slowly.
>> [!success]- Answer
>> b) It calculates values only when they are requested.

> [!question] In the `Product` class, how are attributes accessed inside a method?
> a) `Product.name`
> b) `self.name`
> c) `this.name`
> d) `name`
>> [!success]- Answer
>> b) `self.name`

> [!question] What is the output of `os.makedirs('data', exist_ok=True)` if the 'data' folder already exists?
> a) It deletes the existing folder.
> b) It raises a `FileExistsError`.
> c) It does nothing and the program continues.
> d) It creates a duplicate folder named 'data(1)'.
>> [!success]- Answer
>> c) It does nothing and the program continues.

> [!question] In the string `line = "Monitor,300,4"`, what does `line.split(',')` return?
> a) `"Monitor3004"`
> b) `["Monitor", "300", "4"]`
> c) `("Monitor", 300, 4)`
> d) `["Monitor", 300, 4]` (integers)
>> [!success]- Answer
>> b) `["Monitor", "300", "4"]`

> [!question] Which visualization function is used to add the title "Total Sales per Product"?
> a) `plt.header()`
> b) `plt.name()`
> c) `plt.title()`
> d) `plt.text()`
>> [!success]- Answer
>> c) `plt.title()`

> [!question] The "One Million Products" problem demonstrates that:
> a) Python cannot handle large numbers.
> b) Lists are always faster than generators.
> c) Loading large files entirely into RAM (`readlines`) can crash the system.
> d) Classes are too memory intensive for simple data.
>> [!success]- Answer
>> c) Loading large files entirely into RAM (`readlines`) can crash the system.

> [!question] In the notebook, `sys.path.append('..')` was required because:
> a) The notebook was in the `notebooks/` folder and needed to access `utils/` in the parent root.
> b) The `data.txt` file was missing.
> c) Matplotlib was not installed.
> d) The `os` module was not imported.
>> [!success]- Answer
>> a) The notebook was in the `notebooks/` folder and needed to access `utils/` in the parent root.

> [!question] Match the string method to its functionality.
>> [!example] Group A
>> a) .strip()
>> b) .split(',')
>> c) .upper() (General Python knowledge)
>
>> [!example] Group B
>> n) Separates a string into a list of substrings.
>> o) Converts text to uppercase.
>> p) Removes leading and trailing whitespace.
>
>> [!success]- Answer
>> a) -> p)
>> b) -> n)
>> c) -> o)

> [!question] Match the Class component to its description.
>> [!example] Group A
>> a) Class
>> b) Instance (Object)
>> c) Method
>
>> [!example] Group B
>> n) A specific realization of the blueprint (e.g., specific Laptop).
>> o) A function defined inside a class (e.g., total_value).
>> p) The blueprint or template for creating objects.
>
>> [!success]- Answer
>> a) -> p)
>> b) -> n)
>> c) -> o)

> [!question] Match the variable type conversion used in parsing.
>> [!example] Group A
>> a) Name
>> b) Price
>> c) Quantity
>
>> [!example] Group B
>> n) int()
>> o) float()
>> p) str() (Implicitly kept as string)
>
>> [!success]- Answer
>> a) -> p)
>> b) -> o)
>> c) -> n)

> [!question] Match the file handling mode/method to its action.
>> [!example] Group A
>> a) 'w'
>> b) 'r'
>> c) open()
>
>> [!example] Group B
>> n) Opens a file for reading.
>> o) The function used to access a file on disk.
>> p) Opens a file for writing (overwrites existing).
>
>> [!success]- Answer
>> a) -> p)
>> b) -> n)
>> c) -> o)

> [!question] Match the Loop control keyword to its action.
>> [!example] Group A
>> a) continue
>> b) yield
>> c) return
>
>> [!example] Group B
>> n) Skips the rest of the current loop iteration.
>> o) Exits a function and passes back a value (Standard).
>> p) Pauses a function and produces a value (Generator).
>
>> [!success]- Answer
>> a) -> n)
>> b) -> p)
>> c) -> o)

> [!question] Match the Matplotlib visualization argument to its effect.
>> [!example] Group A
>> a) color='teal'
>> b) alpha=0.7
>> c) linestyle='--'
>
>> [!example] Group B
>> n) Sets the transparency of the element.
>> o) Sets the color of the bars.
>> p) Makes the grid lines dashed.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)
>> c) -> p)

> [!question] Match the Project folder to the file type found inside.
>> [!example] Group A
>> a) utils/
>> b) data/raw/
>> c) notebooks/
>
>> [!example] Group B
>> n) .txt
>> o) .ipynb
>> p) .py
>
>> [!success]- Answer
>> a) -> p)
>> b) -> n)
>> c) -> o)

> [!question] Match the Error Type to the cause.
>> [!example] Group A
>> a) ValueError
>> b) MemoryError (Implied context)
>> c) FileNotFoundError (Implied context)
>
>> [!example] Group B
>> n) Trying to read a file that doesn't exist.
>> o) Trying to convert "abc" to an integer.
>> p) Trying to load 10GB of data into 4GB of RAM.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> p)
>> c) -> n)

> [!question] Match the `Product` attribute to its description.
>> [!example] Group A
>> a) self.name
>> b) self.price
>> c) self.quantity
>
>> [!example] Group B
>> n) The cost of a single unit.
>> o) How many units were sold.
>> p) The identifier of the item.
>
>> [!success]- Answer
>> a) -> p)
>> b) -> n)
>> c) -> o)

> [!question] Match the Helper Function to its primary job.
>> [!example] Group A
>> a) clean_lines
>> b) parse_products
>
>> [!example] Group B
>> n) Converts strings into Tuple structures.
>> o) Removes empty rows and whitespace.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)