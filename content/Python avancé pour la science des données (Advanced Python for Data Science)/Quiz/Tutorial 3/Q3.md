---
sources:
  - "[[Tutorial 03 Python Programming Essentials]]"
---

> [!question] The code `os.makedirs('../data/raw', exist_ok=False)` will crash the program if the directory already exists.
>> [!success]- Answer
>> True

> [!question] In the `Product` class, the `self` parameter in `def total_value(self):` is optional and can be removed without causing errors.
>> [!success]- Answer
>> False

> [!question] The generator function `product_generator` keeps the file open continuously until the entire loop in the main script finishes or breaks.
>> [!success]- Answer
>> True

> [!question] In Matplotlib, the function `plt.xlabel("Product")` changes the label of the vertical Y-axis.
>> [!success]- Answer
>> False

> [!question] The `parse_products` function uses a `try...except` block primarily to handle missing files (FileNotFoundError).
>> [!success]- Answer
>> False

> [!question] The `__init__.py` file is required to make the `utils` directory importable as a package, even if the file itself is empty.
>> [!success]- Answer
>> True

> [!question] When using `plt.bar(names, values)`, the `names` list must contain numerical data for the chart to render correctly.
>> [!success]- Answer
>> False

> [!question] The command `sys.path.append('..')` is a permanent change to the computer's environment variables.
>> [!success]- Answer
>> False

> [!question] In Python, `float('1000')` returns the integer `1000`.
>> [!success]- Answer
>> False

> [!question] The `yield` keyword in Python pauses the function execution and saves its state, allowing it to resume later.
>> [!success]- Answer
>> True

> [!question] What is the primary reason for using `sys.path.append('..')` in the notebook?
> a) To save data to the parent directory.
> b) To allow the notebook to import modules from the `utils` folder located in the parent directory.
> c) To increase the system memory limit.
> d) To move the current working directory to the root.
>> [!success]- Answer
>> b) To allow the notebook to import modules from the `utils` folder located in the parent directory.

> [!question] In the visualization code, what does `bar.get_height()` return?
> a) The width of the bar in pixels.
> b) The numerical value representing the sales total for that product.
> c) The name of the product.
> d) The color code of the bar.
>> [!success]- Answer
>> b) The numerical value representing the sales total for that product.

> [!question] If the input file contains the line `"Laptop,1000,3"`, what is the value of `parts` after running `line.split(',')`?
> a) `"Laptop,1000,3"`
> b) `('Laptop', '1000', '3')`
> c) `['Laptop', '1000', '3']`
> d) `['Laptop', 1000, 3]`
>> [!success]- Answer
>> c) `['Laptop', '1000', '3']`

> [!question] Which of the following is NOT a benefit of using the `Product` class over a simple tuple?
> a) It allows us to attach methods like `total_value()`.
> b) It provides a clearer string representation via `__repr__`.
> c) It uses significantly less memory than a tuple for small datasets.
> d) It encapsulates data and logic in one place.
>> [!success]- Answer
>> c) It uses significantly less memory than a tuple for small datasets.

> [!question] What triggers the execution of code inside a Python generator function?
> a) Defining the function.
> b) Iterating over the generator object (e.g., in a `for` loop).
> c) Importing the module containing the generator.
> d) The `return` statement.
>> [!success]- Answer
>> b) Iterating over the generator object (e.g., in a `for` loop).

> [!question] In the `clean_lines` function, what does the check `if stripped_line:` ensure?
> a) That the line is not just empty whitespace.
> b) That the line contains commas.
> c) That the line is a number.
> d) That the line is longer than 10 characters.
>> [!success]- Answer
>> a) That the line is not just empty whitespace.

> [!question] What is the correct syntax to instantiate a new Product object based on the tutorial's class definition?
> a) `Product.new("Phone", 500, 5)`
> b) `Product["Phone", 500, 5]`
> c) `Product(name="Phone", price=500, quantity=5)`
> d) `create Product("Phone", 500, 5)`
>> [!success]- Answer
>> c) `Product(name="Phone", price=500, quantity=5)`

> [!question] Which Matplotlib function is used to add the dashed horizontal lines behind the bars?
> a) `plt.axhline()`
> b) `plt.grid(axis='y', linestyle='--')`
> c) `plt.background(grid=True)`
> d) `plt.lines(style='dashed')`
>> [!success]- Answer
>> b) `plt.grid(axis='y', linestyle='--')`

> [!question] The tutorial mentions that "Raw text files often contain empty rows". How does the code handle this?
> a) It crashes.
> b) It fills them with zeros.
> c) The `clean_lines` function filters them out using `strip()` and a logic check.
> d) It uses AI to guess the missing data.
>> [!success]- Answer
>> c) The `clean_lines` function filters them out using `strip()` and a logic check.

> [!question] What is the role of `%%writefile ../utils/product.py` in the Jupyter Notebook?
> a) It reads the file into the notebook.
> b) It deletes the file.
> c) It writes the cell content to a specific file on the disk.
> d) It executes the Python code in the cell immediately.
>> [!success]- Answer
>> c) It writes the cell content to a specific file on the disk.

> [!question] Match the Code Variable to its conceptual role in the `Product` class.
>> [!example] Group A
>> a) self.name
>> b) self.total_value()
>> c) self
>
>> [!example] Group B
>> n) A method defining behavior/logic.
>> o) An attribute holding state data.
>> p) Reference to the current instance object.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)
>> c) -> p)

> [!question] Match the Visualization Labeling component to its argument.
>> [!example] Group A
>> a) ha='center'
>> b) va='bottom'
>> c) text
>
>> [!example] Group B
>> n) The actual string to display (e.g., '$3000').
>> o) Horizontal Alignment.
>> p) Vertical Alignment.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> p)
>> c) -> n)

> [!question] Match the File I/O concept to the code used.
>> [!example] Group A
>> a) Context Manager (Auto-close file)
>> b) Read all lines at once
>> c) Write mode
>
>> [!example] Group B
>> n) `with open(...) as f:`
>> o) `'w'`
>> p) `.readlines()`
>
>> [!success]- Answer
>> a) -> n)
>> b) -> p)
>> c) -> o)

> [!question] Match the Data processing step to the function name.
>> [!example] Group A
>> a) clean_lines
>> b) parse_products
>> c) product_generator
>
>> [!example] Group B
>> n) Combines reading, cleaning, and object creation lazily.
>> o) Preprocesses strings to remove noise.
>> p) Converts strings to data tuples.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> p)
>> c) -> n)

> [!question] Match the Library to the functionality used in this tutorial.
>> [!example] Group A
>> a) os
>> b) sys
>> c) matplotlib
>
>> [!example] Group B
>> n) `makedirs`
>> o) `pyplot`
>> p) `path.append`
>
>> [!success]- Answer
>> a) -> n)
>> b) -> p)
>> c) -> o)

> [!question] Match the Python Operator to its usage in the `Product` class.
>> [!example] Group A
>> a) *
>> b) =
>> c) . (dot)
>
>> [!example] Group B
>> n) Attribute access (e.g., p.name).
>> o) Multiplication (Price x Quantity).
>> p) Assignment (Setting values).
>
>> [!success]- Answer
>> a) -> o)
>> b) -> p)
>> c) -> n)

> [!question] Match the String representation to the method that produces it.
>> [!example] Group A
>> a) `Product(name='Laptop', total=3000.0)`
>> b) `<utils.product.Product object at 0x7f8b1c>`
>
>> [!example] Group B
>> n) Default Python object printing (without `__repr__`).
>> o) Custom `__repr__` implementation.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)

> [!question] Match the logical check to its purpose in `parse_products`.
>> [!example] Group A
>> a) `if len(parts) != 3`
>> b) `try: ... except ValueError:`
>
>> [!example] Group B
>> n) Checks for malformed structure (missing columns).
>> o) Checks for data type errors (text instead of numbers).
>
>> [!success]- Answer
>> a) -> n)
>> b) -> o)

> [!question] Match the Plot type to the command.
>> [!example] Group A
>> a) Bar Chart
>> b) Line Chart (Standard)
>> c) Scatter Plot (Standard)
>
>> [!example] Group B
>> n) `plt.plot()`
>> o) `plt.bar()`
>> p) `plt.scatter()` (or `plot` with `linestyle='none'`)
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)
>> c) -> p)

> [!question] Match the Memory strategy to the scenario.
>> [!example] Group A
>> a) List / `readlines()`
>> b) Generator / `yield`
>
>> [!example] Group B
>> n) Small file (10 lines).
>> o) Massive file (1 Million lines).
>
>> [!success]- Answer
>> a) -> n)
>> b) -> o)