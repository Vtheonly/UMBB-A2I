---
sources:
  - "[[03 Pandas Data Analysis]]"
  - "[[08 Pandas Transformation Reference]]"
  - "[[04 Essential Python Libraries Part 01]]"
---

> [!question] The attribute `df.columns` is a callable method (i.e., you must write `df.columns()` to use it).
>> [!success]- Answer
>> False

> [!question] By default, `df.tail()` returns the last 5 rows of the DataFrame.
>> [!success]- Answer
>> True

> [!question] The method `df.info()` prints information about the index dtype, column dtypes, non-null values, and memory usage.
>> [!success]- Answer
>> True

> [!question] When you set `inplace=True` in a method (like `df.drop()`), the method returns `None` instead of a new DataFrame.
>> [!success]- Answer
>> True

> [!question] Pandas broadcasting allows you to assign a single scalar value to an entire column (e.g., `df['NewCol'] = 0`).
>> [!success]- Answer
>> True

> [!question] To access the "year" component of a datetime column, you can use the syntax `df['DateColumn'].year` directly without the `.dt` accessor.
>> [!success]- Answer
>> False

> [!question] The `pd.to_numeric()` function can be used to convert a column of strings (that look like numbers) into actual floats or integers.
>> [!success]- Answer
>> True

> [!question] `df.sample(n=5)` returns the first 5 rows of the DataFrame, similar to `head()`.
>> [!success]- Answer
>> False

> [!question] `df.applymap()` is used to apply a function to every single element in the entire DataFrame (element-wise).
>> [!success]- Answer
>> True

> [!question] `df.corr()` calculates the correlation between string/categorical columns by default.
>> [!success]- Answer
>> False

> [!question] Which method is used to move a specific column into the index (making it the row labels)?
> a) `df.move_to_index()`
> b) `df.set_index()`
> c) `df.make_index()`
> d) `df.index_col()`
>> [!success]- Answer
>> b) `df.set_index()`

> [!question] When saving a DataFrame using `df.to_csv('file.csv')`, which parameter prevents the row numbers (index) from being written into the file?
> a) `no_index=True`
> b) `remove_index=True`
> c) `index=False`
> d) `save_index=False`
>> [!success]- Answer
>> c) `index=False`

> [!question] Which method allows you to cast a column to a specific data type (e.g., from 'int64' to 'float64')?
> a) `df.convert_type()`
> b) `df.cast()`
> c) `df.astype()`
> d) `df.dtype()`
>> [!success]- Answer
>> c) `df.astype()`

> [!question] What is the main difference between `df.count()` and `df.size`?
> a) `count()` includes NaNs, `size` excludes them.
> b) `size` returns the total number of elements (including NaNs), `count()` returns the number of non-null items.
> c) `count()` works only on numeric columns.
> d) There is no difference.
>> [!success]- Answer
>> b) `size` returns the total number of elements (including NaNs), `count()` returns the number of non-null items.

> [!question] Which syntax correctly drops a column named "Price"?
> a) `df.drop("Price")`
> b) `df.drop("Price", axis=1)`
> c) `df.remove("Price")`
> d) `df.delete("Price")`
>> [!success]- Answer
>> b) `df.drop("Price", axis=1)`

> [!question] How do you calculate the cumulative sum of a column?
> a) `df['col'].sum_cumulative()`
> b) `df['col'].total()`
> c) `df['col'].cumsum()`
> d) `df['col'].accumulate()`
>> [!success]- Answer
>> c) `df['col'].cumsum()`

> [!question] Which accessor is required to handle datetime properties (like day, month, year) in a Series?
> a) `.str`
> b) `.dt`
> c) `.date`
> d) `.time`
>> [!success]- Answer
>> b) `.dt`

> [!question] Which function reads a JSON file into a DataFrame?
> a) `pd.load_json()`
> b) `pd.from_json()`
> c) `pd.read_json()`
> d) `pd.get_json()`
>> [!success]- Answer
>> c) `pd.read_json()`

> [!question] To calculate statistics for *all* columns (including strings/objects), what parameter should be added to `describe()`?
> a) `include='all'`
> b) `full=True`
> c) `types='object'`
> d) `force=True`
>> [!success]- Answer
>> a) `include='all'`

> [!question] What does `df.nlargest(3, 'Sales')` do?
> a) Returns the 3 largest columns.
> b) Returns the rows with the top 3 values in the 'Sales' column.
> c) Returns the max value of Sales 3 times.
> d) Sorts the index by size.
>> [!success]- Answer
>> b) Returns the rows with the top 3 values in the 'Sales' column.

> [!question] Match the attribute to the correct Data Type description.
>> [!example] Group A
>> a) object
>> b) float64
>> c) bool
>
>> [!example] Group B
>> n) True or False values.
>> o) Text or mixed numeric/non-numeric values.
>> p) Floating point numbers (decimals).
>
>> [!success]- Answer
>> a) -> o)
>> b) -> p)
>> c) -> n)

> [!question] Match the transformation method to its effect.
>> [!example] Group A
>> a) df.T
>> b) df.reset_index()
>> c) df.set_index()
>
>> [!example] Group B
>> n) Moves the current index into the columns and generates a default integer index.
>> o) Transposes the DataFrame (swaps rows and columns).
>> p) Moves a column into the index (row labels).
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)
>> c) -> p)

> [!question] Match the file export method to the file format.
>> [!example] Group A
>> a) to_excel()
>> b) to_csv()
>> c) to_pickle()
>
>> [!example] Group B
>> n) Comma Separated Values.
>> o) Serialized Python Object (binary format).
>> p) Microsoft Spreadsheet format.
>
>> [!success]- Answer
>> a) -> p)
>> b) -> n)
>> c) -> o)

> [!question] Match the statistical method to its calculation.
>> [!example] Group A
>> a) median()
>> b) mode()
>> c) std()
>
>> [!example] Group B
>> n) The most frequent value.
>> o) The 50th percentile (middle value).
>> p) Standard Deviation (spread).
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)
>> c) -> p)

> [!question] Match the renaming syntax to the component being renamed.
>> [!example] Group A
>> a) `df.rename(columns={'A': 'B'})`
>> b) `df.rename(index={0: 'Start'})`
>
>> [!example] Group B
>> n) Renames the row labels.
>> o) Renames the column headers.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)

> [!question] Match the accessor to the data type it manipulates.
>> [!example] Group A
>> a) .str
>> b) .dt
>
>> [!example] Group B
>> n) Datetime / Timestamp data.
>> o) String / Object data.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)

> [!question] Match the application method to its scope.
>> [!example] Group A
>> a) applymap()
>> b) apply()
>
>> [!example] Group B
>> n) Applies a function along an axis (rows or columns) of the DataFrame.
>> o) Applies a function to every single element in the DataFrame.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)

> [!question] Match the cleaning operation to its result.
>> [!example] Group A
>> a) df.dropna(axis=0)
>> b) df.dropna(axis=1)
>
>> [!example] Group B
>> n) Drops columns containing missing values.
>> o) Drops rows containing missing values.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)

> [!question] Match the method to the rows it returns.
>> [!example] Group A
>> a) head(3)
>> b) tail(3)
>> c) sample(3)
>
>> [!example] Group B
>> n) Last 3 rows.
>> o) Random 3 rows.
>> p) First 3 rows.
>
>> [!success]- Answer
>> a) -> p)
>> b) -> n)
>> c) -> o)

> [!question] Match the plotting accessor to the library backend it typically uses by default in Pandas.
>> [!example] Group A
>> a) df.plot()
>
>> [!example] Group B
>> n) Matplotlib.
>
>> [!success]- Answer
>> a) -> n)