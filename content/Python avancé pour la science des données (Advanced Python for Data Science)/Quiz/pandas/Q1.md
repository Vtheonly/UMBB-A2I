---
sources:
  - "[[03 Pandas Data Analysis]]"
  - "[[08 Pandas Transformation Reference]]"
  - "[[04 Essential Python Libraries Part 01]]"
---

> [!question] A Pandas **Series** is a 2-dimensional labeled data structure, similar to an Excel spreadsheet.
>> [!success]- Answer
>> False

> [!question] The `loc[]` indexer selects data based on **integer positions** (0, 1, 2...), whereas `iloc[]` selects based on **labels**.
>> [!success]- Answer
>> False

> [!question] The method `df.dropna()` removes rows containing missing values (`NaN`) from the DataFrame.
>> [!success]- Answer
>> True

> [!question] `pd.merge()` is primarily used to stack two DataFrames vertically (one on top of the other).
>> [!success]- Answer
>> False

> [!question] Using `inplace=True` in functions like `fillna()` or `drop()` modifies the existing DataFrame directly instead of returning a new one.
>> [!success]- Answer
>> True

> [!question] The `df.describe()` method provides a quick summary of the data types and memory usage of the DataFrame.
>> [!success]- Answer
>> False

> [!question] `pd.cut()` is used to segment and sort continuous data values into discrete bins (categories).
>> [!success]- Answer
>> True

> [!question] The `groupby()` function in Pandas follows the "Split-Apply-Combine" strategy.
>> [!success]- Answer
>> True

> [!question] `df.idxmax()` returns the maximum *value* found in the Series or DataFrame column.
>> [!success]- Answer
>> False

> [!question] The `melt()` function is used to transform a DataFrame from a "wide" format to a "long" format (unpivoting).
>> [!success]- Answer
>> True

> [!question] Which attribute returns a tuple representing the dimensionality of the DataFrame (rows, columns)?
> a) `df.size`
> b) `df.ndim`
> c) `df.shape`
> d) `df.info`
>> [!success]- Answer
>> c) `df.shape`

> [!question] Which function is best suited for reading a comma-separated values file into a DataFrame?
> a) `pd.read_excel()`
> b) `pd.read_table()`
> c) `pd.load_csv()`
> d) `pd.read_csv()`
>> [!success]- Answer
>> d) `pd.read_csv()`

> [!question] What is the difference between `map()` and `apply()`?
> a) `map` is for DataFrames, `apply` is for Series.
> b) `map` is used for element-wise substitution in a Series (using a dict), `apply` works on axes of DataFrames or Series.
> c) `map` is faster than vectorized operations.
> d) There is no difference.
>> [!success]- Answer
>> b) `map` is used for element-wise substitution in a Series (using a dict), `apply` works on axes of DataFrames or Series.

> [!question] How do you select the row with index label "Row5" and column "Age"?
> a) `df.iloc["Row5", "Age"]`
> b) `df.loc["Row5", "Age"]`
> c) `df["Row5"]["Age"]`
> d) `df.at[5, "Age"]`
>> [!success]- Answer
>> b) `df.loc["Row5", "Age"]`

> [!question] Which function combines two DataFrames based on a common key (similar to SQL JOIN)?
> a) `pd.concat()`
> b) `pd.append()`
> c) `pd.join_table()`
> d) `pd.merge()`
>> [!success]- Answer
>> d) `pd.merge()`

> [!question] What does `df['Column'].value_counts()` do?
> a) Sums up all values in the column.
> b) Returns the count of unique values in the column.
> c) Sorts the column values.
> d) Counts the number of missing values.
>> [!success]- Answer
>> b) Returns the count of unique values in the column.

> [!question] If you want to handle duplicate combinations when pivoting data, which function should you use?
> a) `df.pivot()`
> b) `df.pivot_table()`
> c) `df.unstack()`
> d) `df.transpose()`
>> [!success]- Answer
>> b) `df.pivot_table()`

> [!question] Which method allows for scalar access (accessing a single value) and is faster than `loc` or `iloc`?
> a) `df.get()`
> b) `df.ix[]`
> c) `df.at[]`
> d) `df.fast[]`
>> [!success]- Answer
>> c) `df.at[]`

> [!question] What happens if you use `pd.concat([df1, df2], axis=1)`?
> a) It stacks df2 below df1 (vertically).
> b) It attaches df2 to the right of df1 (horizontally).
> c) It merges them based on a common key.
> d) It raises an error.
>> [!success]- Answer
>> b) It attaches df2 to the right of df1 (horizontally).

> [!question] Which function allows you to fill missing values (`NaN`) with a specific value (e.g., the mean)?
> a) `df.dropna()`
> b) `df.fill()`
> c) `df.replace_nan()`
> d) `df.fillna()`
>> [!success]- Answer
>> d) `df.fillna()`

> [!question] Match the Pandas data structure to its description.
>> [!example] Group A
>> a) Series
>> b) DataFrame
>
>> [!example] Group B
>> n) A 2-dimensional labeled data structure (Table).
>> o) A 1-dimensional labeled array.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)

> [!question] Match the inspection method to its output.
>> [!example] Group A
>> a) head()
>> b) info()
>> c) describe()
>
>> [!example] Group B
>> n) Summary statistics (mean, std, min, max) for numerical columns.
>> o) First 5 rows of the DataFrame.
>> p) Summary of columns, non-null counts, and data types.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> p)
>> c) -> n)

> [!question] Match the indexing method to its logic.
>> [!example] Group A
>> a) loc[]
>> b) iloc[]
>
>> [!example] Group B
>> n) Integer-based position (0 to length-1).
>> o) Label-based indexing (Row Names, Column Names).
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)

> [!question] Match the grouping step to the "Split-Apply-Combine" concept.
>> [!example] Group A
>> a) Split
>> b) Apply
>> c) Combine
>
>> [!example] Group B
>> n) Putting the results back together into a result object.
>> o) Breaking data into groups based on criteria.
>> p) Applying a function (sum, mean) to each group independently.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> p)
>> c) -> n)

> [!question] Match the merging function to its behavior.
>> [!example] Group A
>> a) pd.concat()
>> b) pd.merge()
>
>> [!example] Group B
>> n) Joins data based on common columns or indices (Relational/SQL style).
>> o) Glues data together along an axis (usually stacking).
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)

> [!question] Match the handling of missing data to the function.
>> [!example] Group A
>> a) dropna()
>> b) fillna()
>> c) isna()
>
>> [!example] Group B
>> n) Detects missing values (returns boolean).
>> o) Removes rows or columns with missing values.
>> p) Replaces missing values with a specified value.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> p)
>> c) -> n)

> [!question] Match the file export parameter to its effect.
>> [!example] Group A
>> a) index=False
>> b) encoding='utf-8'
>
>> [!example] Group B
>> n) Ensures special characters are saved correctly.
>> o) Prevents the row numbers (index) from being written to the file.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)

> [!question] Match the reshaping function to its description.
>> [!example] Group A
>> a) pivot()
>> b) melt()
>
>> [!example] Group B
>> n) Reshapes data from wide to long format (Unpivot).
>> o) Reshapes data based on column values (Spreadsheet style).
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)

> [!question] Match the ranking/sorting method to the behavior.
>> [!example] Group A
>> a) sort_values()
>> b) rank()
>
>> [!example] Group B
>> n) Assigns a numerical rank (1st, 2nd, 3rd) to entries.
>> o) Reorders the rows based on the values in a column.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)

> [!question] Match the logic function to the example.
>> [!example] Group A
>> a) np.where()
>> b) map()
>
>> [!example] Group B
>> n) `df['Sex'].map({'M': 0, 'F': 1})`
>> o) `np.where(df['Age'] > 18, 'Adult', 'Child')`
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)