---
sources:
  - "[[03 Pandas Data Analysis]]"
  - "[[08 Pandas Transformation Reference]]"
  - "[[04 Essential Python Libraries Part 01]]"
---

> [!question] `df.loc[1:3]` and `df.iloc[1:3]` produce the exact same result if the DataFrame has a standard integer index (0, 1, 2...).
>> [!success]- Answer
>> False

> [!question] The `groupby()` function returns a new DataFrame immediately, without needing an aggregation function like `sum()` or `mean()`.
>> [!success]- Answer
>> False

> [!question] `pd.concat([df1, df2], axis=0)` stacks `df2` below `df1` (adding rows).
>> [!success]- Answer
>> True

> [!question] The `reset_index()` method moves the current index into a regular column and establishes a new default integer index.
>> [!success]- Answer
>> True

> [!question] `df.query('Age > 25')` is a valid way to filter rows using a string expression in Pandas.
>> [!success]- Answer
>> True

> [!question] In a `pd.pivot_table()`, if you do not specify an `aggfunc`, it defaults to `sum`.
>> [!success]- Answer
>> False

> [!question] `df.drop('ColumnName')` will delete a column by default without needing to specify `axis=1`.
>> [!success]- Answer
>> False

> [!question] `df.sort_values()` can sort by multiple columns if you pass a list of column names.
>> [!success]- Answer
>> True

> [!question] When using `pd.read_csv()`, the parameter `index_col=0` tells Pandas to use the first column of the CSV as the row index.
>> [!success]- Answer
>> True

> [!question] `df.duplicated()` returns a new DataFrame with all duplicate rows removed.
>> [!success]- Answer
>> False

> [!question] Which method allows you to rename specific columns using a dictionary?
> a) `df.change_names()`
> b) `df.replace()`
> c) `df.rename()`
> d) `df.set_names()`
>> [!success]- Answer
>> c) `df.rename()`

> [!question] What is the result of `df['Col'].unique()`?
> a) A count of unique values.
> b) A DataFrame with duplicates removed.
> c) An array of distinct values found in the Series.
> d) A boolean Series indicating uniqueness.
>> [!success]- Answer
>> c) An array of distinct values found in the Series.

> [!question] Which function determines the number of missing (NaN) values in each column?
> a) `df.count()`
> b) `df.isnull().sum()`
> c) `df.missing()`
> d) `df.nan_count()`
>> [!success]- Answer
>> b) `df.isnull().sum()`

> [!question] In `pd.merge(df1, df2, how='left')`, what happens to rows in `df1` that have no match in `df2`?
> a) They are discarded.
> b) They are kept, with `NaN` filled in for `df2`'s columns.
> c) They cause an error.
> d) They are filled with zeros.
>> [!success]- Answer
>> b) They are kept, with `NaN` filled in for `df2`'s columns.

> [!question] Which string method is used to access string manipulation functions on a Series (e.g., to uppercase)?
> a) `df['Col'].string.upper()`
> b) `df['Col'].str.upper()`
> c) `df['Col'].upper()`
> d) `df['Col'].text.upper()`
>> [!success]- Answer
>> b) `df['Col'].str.upper()`

> [!question] What is the key difference between `pivot()` and `pivot_table()`?
> a) `pivot_table` is deprecated.
> b) `pivot` can handle duplicate index/column pairs by aggregating, `pivot_table` cannot.
> c) `pivot_table` can handle duplicate index/column pairs by aggregating, `pivot` fails.
> d) There is no difference.
>> [!success]- Answer
>> c) `pivot_table` can handle duplicate index/column pairs by aggregating, `pivot` fails.

> [!question] How do you sort a DataFrame by its Index?
> a) `df.sort_values(by='index')`
> b) `df.sort_index()`
> c) `df.order_by_index()`
> d) `df.rank_index()`
>> [!success]- Answer
>> b) `df.sort_index()`

> [!question] Which parameter in `pd.read_csv` handles dates automatically?
> a) `parse_dates`
> b) `convert_dates`
> c) `read_dates`
> d) `date_parser`
>> [!success]- Answer
>> a) `parse_dates`

> [!question] What does `df.select_dtypes(include='number')` do?
> a) Returns a count of numeric rows.
> b) Converts all columns to numbers.
> c) Returns a subset DataFrame containing only numeric columns.
> d) Checks if the DataFrame is empty.
>> [!success]- Answer
>> c) Returns a subset DataFrame containing only numeric columns.

> [!question] To access the underlying NumPy array of a DataFrame `df`, which attribute do you use?
> a) `df.array`
> b) `df.to_numpy`
> c) `df.values`
> d) `df.data`
>> [!success]- Answer
>> c) `df.values`

> [!question] Match the attribute to the information it provides.
>> [!example] Group A
>> a) df.columns
>> b) df.index
>> c) df.dtypes
>
>> [!example] Group B
>> n) The data types of each column.
>> o) The labels of the rows.
>> p) The labels of the columns.
>
>> [!success]- Answer
>> a) -> p)
>> b) -> o)
>> c) -> n)

> [!question] Match the join type to its SQL equivalent behavior.
>> [!example] Group A
>> a) how='inner'
>> b) how='outer'
>> c) how='left'
>
>> [!example] Group B
>> n) Keep all rows from the left table, match if possible.
>> o) Keep only rows present in both tables.
>> p) Keep all rows from both tables, filling NaNs where missing.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> p)
>> c) -> n)

> [!question] Match the aggregation method to its function.
>> [!example] Group A
>> a) count()
>> b) value_counts()
>
>> [!example] Group B
>> n) Returns the frequency of unique values in a Series.
>> o) Returns the number of non-null entries in each DataFrame column.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)

> [!question] Match the selection syntax to the return type (assuming standard use).
>> [!example] Group A
>> a) `df['Column']`
>> b) `df[['Column']]`
>
>> [!example] Group B
>> n) Returns a DataFrame (preserving 2D structure).
>> o) Returns a Series (1D structure).
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)

> [!question] Match the duplicate function to its action.
>> [!example] Group A
>> a) duplicated()
>> b) drop_duplicates()
>
>> [!example] Group B
>> n) Returns the DataFrame without duplicate rows.
>> o) Returns a boolean Series marking duplicate rows.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)

> [!question] Match the axis number to the direction in Pandas.
>> [!example] Group A
>> a) axis=0
>> b) axis=1
>
>> [!example] Group B
>> n) Columns (Horizontal / "Along the columns").
>> o) Rows (Vertical / "Along the index").
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)

> [!question] Match the string method to its operation.
>> [!example] Group A
>> a) str.contains()
>> b) str.replace()
>
>> [!example] Group B
>> n) Substitutes a substring with another string.
>> o) Checks if a pattern exists in the string (returns True/False).
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)

> [!question] Match the math operation to the method.
>> [!example] Group A
>> a) Cumulative Sum
>> b) Correlation Matrix
>
>> [!example] Group B
>> n) `df.corr()`
>> o) `df.cumsum()`
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)

> [!question] Match the binning function to its logic.
>> [!example] Group A
>> a) pd.cut()
>> b) pd.qcut()
>
>> [!example] Group B
>> n) Bins data based on specified value edges (e.g., 0-10, 10-20).
>> o) Bins data based on quantiles (e.g., equal number of items per bin).
>
>> [!success]- Answer
>> a) -> n)
>> b) -> o)

> [!question] Match the boolean operator to its Pandas/NumPy syntax.
>> [!example] Group A
>> a) AND
>> b) OR
>> c) NOT
>
>> [!example] Group B
>> n) `~`
>> o) `&`
>> p) `|`
>
>> [!success]- Answer
>> a) -> o)
>> b) -> p)
>> c) -> n)