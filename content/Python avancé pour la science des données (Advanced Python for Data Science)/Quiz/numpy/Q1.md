---
sources:
  - "[[02 NumPy Deep Dive]]"
  - "[[07 NumPy Function Reference]]"
  - "[[06 Python Core Mechanics]]"
---

> [!question] NumPy arrays are homogeneous, meaning they must contain elements of the same data type.
>> [!success]- Answer
>> True

> [!question] In a 2D NumPy array, Axis 0 represents moving "across the rows" (horizontally).
>> [!success]- Answer
>> False

> [!question] NumPy arrays store data in scattered memory blocks, similar to Python lists, which makes them flexible but slower.
>> [!success]- Answer
>> False

> [!question] When using `np.arange(start, stop)`, the `stop` value is included in the generated array.
>> [!success]- Answer
>> False

> [!question] To perform an element-wise "AND" condition in Boolean masking, you must use the `&` symbol, not the word `and`.
>> [!success]- Answer
>> True

> [!question] `np.random.seed(42)` ensures that the random numbers generated are the exact same every time the script is run.
>> [!success]- Answer
>> True

> [!question] `np.argmax(array)` returns the maximum value found inside the array.
>> [!success]- Answer
>> False

> [!question] The function `np.vstack` adds a new column to the right of an existing array.
>> [!success]- Answer
>> False

> [!question] `np.maximum(arr1, arr2)` compares two arrays element-by-element, whereas `np.max(arr1)` finds the single largest value in one array.
>> [!success]- Answer
>> True

> [!question] `np.all(condition, axis=1)` checks if at least one value in a row satisfies the condition.
>> [!success]- Answer
>> False

> [!question] Which of the following best describes why NumPy arrays are faster than Python lists?
> a) They use more memory to cache results.
> b) They use contiguous blocks of memory and allow vectorized operations.
> c) They are heterogeneous and can store any data type.
> d) They do not require importing external libraries.
>> [!success]- Answer
>> b) They use contiguous blocks of memory and allow vectorized operations.

> [!question] If you have a 2D matrix, which axis would you perform a reduction on to calculate the average of each **column**?
> a) Axis 1
> b) Axis -1
> c) Axis 0
> d) Axis 2
>> [!success]- Answer
>> c) Axis 0

> [!question] What is the correct syntax for a boolean mask checking if an array `A` is greater than 5 AND less than 10?
> a) `A > 5 and A < 10`
> b) `(A > 5) & (A < 10)`
> c) `A > 5 & A < 10`
> d) `(A > 5) | (A < 10)`
>> [!success]- Answer
>> b) `(A > 5) & (A < 10)`

> [!question] What does `np.arange(1, 5)` return?
> a) `[1, 2, 3, 4, 5]`
> b) `[1, 2, 3, 4]`
> c) `[0, 1, 2, 3, 4]`
> d) `[2, 3, 4, 5]`
>> [!success]- Answer
>> b) `[1, 2, 3, 4]`

> [!question] Which function converts a flat index (returned by argmax) back into Coordinate Geometry (Row, Column)?
> a) `np.reshape`
> b) `np.ravel`
> c) `np.unravel_index`
> d) `np.resize`
>> [!success]- Answer
>> c) `np.unravel_index`

> [!question] What does `np.argsort([10, 2, 5])` return?
> a) `[2, 5, 10]`
> b) `[10, 5, 2]`
> c) `[1, 2, 0]`
> d) `[0, 1, 2]`
>> [!success]- Answer
>> c) `[1, 2, 0]`

> [!question] What is the result of `np.random.randint(0, 5, size=(2, 2))`?
> a) A 2x2 matrix with floats between 0 and 5.
> b) A list of 5 random integers.
> c) A 2x2 matrix with integers from 0 to 4.
> d) A 2x2 matrix with integers from 0 to 5.
>> [!success]- Answer
>> c) A 2x2 matrix with integers from 0 to 4.

> [!question] Which function allows you to "glue" two arrays together top-to-bottom (vertically)?
> a) `np.hstack`
> b) `np.concat_cols`
> c) `np.vstack`
> d) `np.append_rows`
>> [!success]- Answer
>> c) `np.vstack`

> [!question] What is the 50th percentile of a dataset also known as?
> a) The Mean
> b) The Median
> c) The Mode
> d) The Standard Deviation
>> [!success]- Answer
>> b) The Median

> [!question] Which operator is used for the "OR" condition in NumPy boolean masking?
> a) `or`
> b) `||`
> c) `!`
> d) `|`
>> [!success]- Answer
>> d) `|`

> [!question] Match the axis to its visual direction in a 2D Array.
>> [!example] Group A
>> a) Axis 0
>> b) Axis 1
>
>> [!example] Group B
>> n) Across the rows (Horizontal)
>> o) Down the columns (Vertical)
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)

> [!question] Match the stacking function to its description.
>> [!example] Group A
>> a) np.vstack
>> b) np.hstack
>
>> [!example] Group B
>> n) Adds a new column to the right.
>> o) Adds a new row to the bottom.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)

> [!question] Match the finding function to its return value.
>> [!example] Group A
>> a) np.argmax
>> b) np.max
>
>> [!example] Group B
>> n) Returns the highest value in the array.
>> o) Returns the index (position) of the highest value.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)

> [!question] Match the logical operator to its NumPy boolean symbol.
>> [!example] Group A
>> a) AND
>> b) OR
>
>> [!example] Group B
>> n) `|`
>> o) `&`
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)

> [!question] Match the array creation function to its behavior.
>> [!example] Group A
>> a) np.arange(0, 5)
>> b) np.random.randint(0, 5)
>
>> [!example] Group B
>> n) Generates integers where the 'high' limit is exclusive.
>> o) Generates a range where the 'stop' number is exclusive.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)

> [!question] Match the Python structure to the NumPy equivalent behavior.
>> [!example] Group A
>> a) List
>> b) Ndarray
>
>> [!example] Group B
>> n) Homogeneous, contiguous memory, vectorized.
>> o) Heterogeneous, scattered memory, requires loops.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)

> [!question] Match the comparison function to its definition.
>> [!example] Group A
>> a) np.maximum
>> b) np.all
>
>> [!example] Group B
>> n) Checks if every element satisfies a condition.
>> o) Compares two arrays element-wise and picks the winner.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)

> [!question] Match the advanced indexing function to its purpose.
>> [!example] Group A
>> a) np.unravel_index
>> b) np.argsort
>
>> [!example] Group B
>> n) Returns indices that would sort the array.
>> o) Converts a flat index back into Row/Column coordinates.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)

> [!question] Match the random module function to its functionality.
>> [!example] Group A
>> a) np.random.seed
>> b) np.random.rand
>
>> [!example] Group B
>> n) Generates pseudo-random numbers from a uniform distribution.
>> o) Sets the starting point to guarantee reproducibility.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)

> [!question] Match the percentile value to its statistical name.
>> [!example] Group A
>> a) 50th Percentile
>> b) 25th Percentile
>
>> [!example] Group B
>> n) Lower Quartile
>> o) Median
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)