---
sources:
  - "[[07 NumPy Function Reference]]"
  - "[[04 Essential Python Libraries Part 01]]"
---

> [!question] `np.arange(0, 10, 2)` generates the array `[0, 2, 4, 6, 8, 10]`.
>> [!success]- Answer
>> False

> [!question] The function `np.empty((2, 2))` creates a matrix explicitly filled with zeros.
>> [!success]- Answer
>> False

> [!question] `np.identity(3)` and `np.eye(3)` both produce a 3x3 Identity Matrix (ones on the diagonal).
>> [!success]- Answer
>> True

> [!question] Broadcasting allows NumPy to perform arithmetic operations on arrays of different shapes, such as adding a scalar to a matrix.
>> [!success]- Answer
>> True

> [!question] The attribute `arr.nbytes` calculates the total memory consumed by the array (number of elements * bytes per element).
>> [!success]- Answer
>> True

> [!question] `np.fromfunction` creates an array by executing a function over each coordinate (index) of the grid.
>> [!success]- Answer
>> True

> [!question] `np.random.randint(1, 10)` generates an integer including 10 (inclusive high).
>> [!success]- Answer
>> False

> [!question] `np.trace(A)` computes the sum of the diagonal elements of a matrix.
>> [!success]- Answer
>> True

> [!question] To calculate the eigenvalues and eigenvectors of a matrix, you use `np.linalg.solve()`.
>> [!success]- Answer
>> False

> [!question] `np.concatenate` can join two arrays along a specified axis, provided their shapes match in the other dimensions.
>> [!success]- Answer
>> True

> [!question] What is the return value of `np.array([True, True, False]).sum()`?
> a) `True`
> b) `2`
> c) `3`
> d) `[1, 1, 0]`
>> [!success]- Answer
>> b) `2`

> [!question] Which function constructs a new array from an iterable object?
> a) `np.iterable()`
> b) `np.fromlist()`
> c) `np.fromiter()`
> d) `np.create()`
>> [!success]- Answer
>> c) `np.fromiter()`

> [!question] Which attribute tells you the memory size of **one single element** in the array in bytes?
> a) `arr.size`
> b) `arr.nbytes`
> c) `arr.itemsize`
> d) `arr.shape`
>> [!success]- Answer
>> c) `arr.itemsize`

> [!question] What does `np.linspace(0, 10, 5)` produce?
> a) `[0, 2, 4, 6, 8]`
> b) `[0. , 2.5, 5. , 7.5, 10. ]`
> c) `[0, 1, 2, 3, 4]`
> d) `[0, 10, 20, 30, 40]`
>> [!success]- Answer
>> b) `[0. , 2.5, 5. , 7.5, 10. ]`

> [!question] Which function calculates the magnitude (or vector norm) of a vector or matrix?
> a) `np.abs()`
> b) `np.linalg.norm()`
> c) `np.linalg.mag()`
> d) `np.linalg.rank()`
>> [!success]- Answer
>> b) `np.linalg.norm()`

> [!question] What is the output of `np.full((2, 3), 9)`?
> a) A 2x3 matrix filled with the number 9.
> b) A 2x3 matrix filled with random numbers up to 9.
> c) A 1D array with 9 elements.
> d) A 9x9 matrix filled with zeros.
>> [!success]- Answer
>> a) A 2x3 matrix filled with the number 9.

> [!question] Which NumPy function solves a linear matrix equation (like finding x in Ax = b)?
> a) `np.linalg.eig()`
> b) `np.linalg.inv()`
> c) `np.linalg.solve()`
> d) `np.linalg.root()`
>> [!success]- Answer
>> c) `np.linalg.solve()`

> [!question] What does `np.random.choice(['Red', 'Blue'], size=3)` do?
> a) Picks one random color 3 times.
> b) Creates a list `['Red', 'Blue', 'Red', 'Blue', 'Red', 'Blue']`.
> c) Selects 3 unique colors (will error here since only 2 exist).
> d) Randomly shuffles the list 3 times.
>> [!success]- Answer
>> a) Picks one random color 3 times.

> [!question] If `a = np.array([10, 20, 30])` and `b = 5`, what is `a + b`?
> a) Error: Shapes do not match.
> b) `[15, 20, 30]` (Adds to first element only).
> c) `[10, 20, 30, 5]` (Appends 5).
> d) `[15, 25, 35]` (Broadcasting).
>> [!success]- Answer
>> d) `[15, 25, 35]` (Broadcasting).

> [!question] How do you convert a 1D array of 12 elements into a 3x4 matrix?
> a) `arr.resize(3, 4)`
> b) `arr.reshape(3, 4)`
> c) `arr.view(3, 4)`
> d) `arr.split(3, 4)`
>> [!success]- Answer
>> b) `arr.reshape(3, 4)`

> [!question] Match the attribute to its meaning.
>> [!example] Group A
>> a) itemsize
>> b) nbytes
>
>> [!example] Group B
>> n) Total bytes consumed by the entire array.
>> o) Bytes consumed by a single element.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)

> [!question] Match the Linear Algebra function to its purpose.
>> [!example] Group A
>> a) np.trace
>> b) np.linalg.eig
>> c) np.linalg.matrix_rank
>
>> [!example] Group B
>> n) Returns eigenvalues and eigenvectors.
>> o) Sum of diagonal elements.
>> p) Rank of the matrix.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)
>> c) -> p)

> [!question] Match the random function to its behavior.
>> [!example] Group A
>> a) np.random.choice
>> b) np.random.randint
>
>> [!example] Group B
>> n) Generates random integers within a range.
>> o) Selects random elements from a given 1D array or list.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)

> [!question] Match the array creation function to its description.
>> [!example] Group A
>> a) np.full
>> b) np.eye
>> c) np.fromfunction
>
>> [!example] Group B
>> n) Creates an array from a function applied to indices.
>> o) Creates a matrix filled with a specific value.
>> p) Creates an identity matrix.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> p)
>> c) -> n)

> [!question] Match the stacking/concatenation tool to its axis.
>> [!example] Group A
>> a) np.vstack
>> b) np.hstack
>
>> [!example] Group B
>> n) Equivalent to concatenation along axis 1 (columns).
>> o) Equivalent to concatenation along axis 0 (rows).
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)

> [!question] Match the slicing operation to the result on `arr = [0, 1, 2, 3]`.
>> [!example] Group A
>> a) `arr[::-1]`
>> b) `arr[::2]`
>
>> [!example] Group B
>> n) `[0, 2]` (Every 2nd item)
>> o) `[3, 2, 1, 0]` (Reversed)
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)

> [!question] Match the math constant/function to the NumPy syntax.
>> [!example] Group A
>> a) Square Root
>> b) Natural Logarithm
>> c) Exponential (e^x)
>
>> [!example] Group B
>> n) `np.log()`
>> o) `np.exp()`
>> p) `np.sqrt()`
>
>> [!success]- Answer
>> a) -> p)
>> b) -> n)
>> c) -> o)

> [!question] Match the shape manipulation to its definition.
>> [!example] Group A
>> a) Flattening
>> b) Reshaping
>
>> [!example] Group B
>> n) Changing dimensions (e.g., 2x3 to 3x2) while keeping data count.
>> o) Converting a multi-dimensional array into 1D.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)

> [!question] Match the comparison result to the operation `np.array([1, 2]) > 1`.
>> [!example] Group A
>> a) Result Array
>> b) Result Type
>
>> [!example] Group B
>> n) `[False, True]`
>> o) Boolean (`bool`)
>
>> [!success]- Answer
>> a) -> n)
>> b) -> o)

> [!question] Match the special matrix creation to the function.
>> [!example] Group A
>> a) Diagonal of ones
>> b) All zeros
>> c) All ones
>
>> [!example] Group B
>> n) `np.ones()`
>> o) `np.eye()`
>> p) `np.zeros()`
>
>> [!success]- Answer
>> a) -> o)
>> b) -> p)
>> c) -> n)