---
sources:
  - "[[02 NumPy Deep Dive]]"
  - "[[07 NumPy Function Reference]]"
  - "[[04 Essential Python Libraries Part 01]]"
---

> [!question] When using `np.array([1, 2]) * 2`, the result is `[1, 2, 1, 2]`, similar to how Python lists behave.
>> [!success]- Answer
>> False

> [!question] The attribute `ndarray.ndim` returns the total number of elements in the array.
>> [!success]- Answer
>> False

> [!question] The `np.linspace(start, stop, num)` function includes the `stop` value by default in the generated array.
>> [!success]- Answer
>> True

> [!question] You can perform matrix multiplication in NumPy using the `np.dot()` function or the `@` operator.
>> [!success]- Answer
>> True

> [!question] `np.zeros((3, 3))` creates a 3x3 matrix where the default data type of the elements is integer (`int`).
>> [!success]- Answer
>> False

> [!question] Slicing an array with `arr[::-1]` will reverse the order of elements.
>> [!success]- Answer
>> True

> [!question] To calculate the standard deviation of an array, you use the function `np.var()`.
>> [!success]- Answer
>> False

> [!question] `np.random.rand(d0, d1)` generates random numbers from a "Normal" (Gaussian) distribution.
>> [!success]- Answer
>> False

> [!question] The `reshape` method can change the dimensions of an array without changing its data, as long as the total number of elements remains the same.
>> [!success]- Answer
>> True

> [!question] When using `np.where(condition)`, if you do not provide `value_if_true` and `value_if_false`, it returns the indices where the condition is True.
>> [!success]- Answer
>> True

> [!question] Which NumPy attribute would you use to find the tuple representing the dimensions of the array (e.g., rows and columns)?
> a) `arr.ndim`
> b) `arr.size`
> c) `arr.shape`
> d) `arr.dtype`
>> [!success]- Answer
>> c) `arr.shape`

> [!question] What is the result of `np.arange(1, 6, 2)`?
> a) `[1, 2, 3, 4, 5, 6]`
> b) `[1, 3, 5]`
> c) `[1, 3, 5, 7]`
> d) `[2, 4, 6]`
>> [!success]- Answer
>> b) `[1, 3, 5]`

> [!question] Which function creates an Identity Matrix (a square matrix with ones on the diagonal and zeros elsewhere)?
> a) `np.identity_matrix()`
> b) `np.ones()`
> c) `np.eye()`
> d) `np.diag()`
>> [!success]- Answer
>> c) `np.eye()`

> [!question] If you have a 2D array `arr`, how do you select all rows from the second column?
> a) `arr[1, :]`
> b) `arr[:, 1]`
> c) `arr[0:2]`
> d) `arr[1]`
>> [!success]- Answer
>> b) `arr[:, 1]`

> [!question] What does `np.full((2, 2), 7)` produce?
> a) A 2x2 matrix filled with the number 7.
> b) A 1D array with values `[2, 2, 7]`.
> c) A matrix filled with numbers from 0 to 7.
> d) An empty 2x2 matrix.
>> [!success]- Answer
>> a) A 2x2 matrix filled with the number 7.

> [!question] Which aggregation function calculates the square root of every element in an array?
> a) `np.power()`
> b) `np.sqrt()`
> c) `np.sq()`
> d) `np.root()`
>> [!success]- Answer
>> b) `np.sqrt()`

> [!question] How do you transpose a matrix `arr` (swap rows and columns)?
> a) `arr.swap()`
> b) `arr.T`
> c) `np.moveaxis(arr)`
> d) `arr.inverse()`
>> [!success]- Answer
>> b) `arr.T`

> [!question] What is the primary difference between `np.random.rand()` and `np.random.randn()`?
> a) `rand` is for integers, `randn` is for floats.
> b) `rand` is Uniform distribution [0, 1), `randn` is Normal (Gaussian) distribution.
> c) `rand` uses a seed, `randn` does not.
> d) There is no difference.
>> [!success]- Answer
>> b) `rand` is Uniform distribution [0, 1), `randn` is Normal (Gaussian) distribution.

> [!question] If you want to find the index of the minimum value in an array, which function do you use?
> a) `np.min()`
> b) `np.argmin()`
> c) `np.sort()`
> d) `np.lowest()`
>> [!success]- Answer
>> b) `np.argmin()`

> [!question] Which method is used to cast an array to a different data type (e.g., from float to int)?
> a) `arr.convert()`
> b) `arr.dtype()`
> c) `arr.astype()`
> d) `arr.cast()`
>> [!success]- Answer
>> c) `arr.astype()`

> [!question] Match the array inspection attribute to its description.
>> [!example] Group A
>> a) ndim
>> b) size
>> c) dtype
>
>> [!example] Group B
>> n) The total number of elements in the array.
>> o) The data type of the elements (e.g., int64, float32).
>> p) The number of dimensions (axes) of the array.
>
>> [!success]- Answer
>> a) -> p)
>> b) -> n)
>> c) -> o)

> [!question] Match the creation function to its output type.
>> [!example] Group A
>> a) np.zeros((2,2))
>> b) np.ones((2,2))
>> c) np.empty((2,2))
>
>> [!example] Group B
>> n) Array filled with 1s.
>> o) Array filled with uninitialized (random memory) values.
>> p) Array filled with 0s.
>
>> [!success]- Answer
>> a) -> p)
>> b) -> n)
>> c) -> o)

> [!question] Match the mathematical operation to its symbol/function.
>> [!example] Group A
>> a) Element-wise addition
>> b) Matrix Multiplication
>> c) Power/Exponentiation
>
>> [!example] Group B
>> n) `arr1 + arr2`
>> o) `arr1 ** 2`
>> p) `np.dot(arr1, arr2)`
>
>> [!success]- Answer
>> a) -> n)
>> b) -> p)
>> c) -> o)

> [!question] Match the statistical function to its description.
>> [!example] Group A
>> a) np.mean()
>> b) np.std()
>> c) np.sum()
>
>> [!example] Group B
>> n) Calculates the total of all elements.
>> o) Calculates the average.
>> p) Calculates the standard deviation.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> p)
>> c) -> n)

> [!question] Match the slicing syntax to its effect.
>> [!example] Group A
>> a) `arr[0:5]`
>> b) `arr[:5]`
>> c) `arr[5:]`
>
>> [!example] Group B
>> n) Selects elements from index 0 up to (but not including) 5.
>> o) Selects elements from index 5 to the end.
>> p) Equivalent to `arr[0:5]` (implicit start at 0).
>
>> [!success]- Answer
>> a) -> n)
>> b) -> p)
>> c) -> o)

> [!question] Match the logic function to its behavior.
>> [!example] Group A
>> a) np.any()
>> b) np.all()
>
>> [!example] Group B
>> n) Returns True if *at least one* element satisfies the condition.
>> o) Returns True only if *every* element satisfies the condition.
>
>> [!success]- Answer
>> a) -> n)
>> b) -> o)

> [!question] Match the Reshaping function to its behavior.
>> [!example] Group A
>> a) ravel()
>> b) reshape()
>
>> [!example] Group B
>> n) Changes the dimensions (e.g., 1D to 2D) without copying data if possible.
>> o) Flattens a multi-dimensional array into a 1D array.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)

> [!question] Match the Linear Algebra function to its result.
>> [!example] Group A
>> a) np.linalg.inv()
>> b) np.linalg.det()
>
>> [!example] Group B
>> n) Calculates the determinant of a matrix.
>> o) Calculates the inverse of a matrix.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)

> [!question] Match the axis parameter to the aggregation direction.
>> [!example] Group A
>> a) sum(axis=0)
>> b) sum(axis=1)
>
>> [!example] Group B
>> n) Sums across rows (collapses columns).
>> o) Sums down columns (collapses rows).
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)

> [!question] Match the indexing method to the example.
>> [!example] Group A
>> a) Integer Indexing
>> b) Boolean Indexing
>
>> [!example] Group B
>> n) `arr[arr > 5]`
>> o) `arr[2]`
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)