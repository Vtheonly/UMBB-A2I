---
sources:
  - "[[Tutorial 02 Hospital Patient Data Analysis]]"
---

> [!question] In the notebook code, `%pip install pandas matplotlib` is used to install libraries directly from within the Jupyter environment.
>> [!success]- Answer
>> True

> [!question] The analogy provided states that a Data Scientist builds the "roads" and "databases" (the race track).
>> [!success]- Answer
>> False

> [!question] The command `plt.grid(True)` removes the grid lines from the chart to make it cleaner.
>> [!success]- Answer
>> False

> [!question] The project structure isolates the Python binary inside the `env/` folder so that updates elsewhere on the computer do not break the project.
>> [!success]- Answer
>> True

> [!question] `df.describe()` provides the "count" of rows, which helps identify if there are missing values (NaN) in numerical columns.
>> [!success]- Answer
>> True

> [!question] The `notebooks/` folder is designed to store the final production-ready automation scripts (`.py` files).
>> [!success]- Answer
>> False

> [!question] In the Python code provided, `os` is imported primarily to perform complex mathematical calculations.
>> [!success]- Answer
>> False

> [!question] A scatter plot is created in Matplotlib by setting `linestyle='none'` to prevent the points from being connected.
>> [!success]- Answer
>> True

> [!question] Git is described as a version control system that creates a timeline of changes known as "commits".
>> [!success]- Answer
>> True

> [!question] The tutorial suggests that `pandas` is used for visualization, while `matplotlib` is used for loading CSV files.
>> [!success]- Answer
>> False

> [!question] In the code block `if not os.path.exists(file_path):`, what happens if the file is missing?
> a) It downloads the file from the internet.
> b) It prints an error message to the user.
> c) It crashes the Python interpreter immediately.
> d) It creates a fake dataset automatically.
>> [!success]- Answer
>> b) It prints an error message to the user.

> [!question] Which Matplotlib argument controls the color of the data points in the scatter plot?
> a) c='blue'
> b) color='b'
> c) hue='b'
> d) palette='blue'
>> [!success]- Answer
>> b) color='b'

> [!question] What is the primary focus of an ML Engineer according to the text?
> a) Moving terabytes of data.
> b) Descriptive statistics of the past.
> c) Mathematical accuracy and model latency.
> d) Creating Jupyter Notebooks.
>> [!success]- Answer
>> c) Mathematical accuracy and model latency.

> [!question] Which column in the dataset is used as the X-axis in the visualization?
> a) Cholesterol
> b) Diagnosis
> c) Sex
> d) Age
>> [!success]- Answer
>> d) Age

> [!question] Why is it considered dangerous to install libraries globally?
> a) It takes up too much hard drive space.
> b) It slows down the internet connection.
> c) Different projects may require conflicting versions of the same library.
> d) Git cannot track global libraries.
>> [!success]- Answer
>> c) Different projects may require conflicting versions of the same library.

> [!question] What does the `alpha=0.6` parameter do in the `plt.grid()` function?
> a) It changes the grid color to red.
> b) It sets the transparency of the grid lines.
> c) It increases the thickness of the lines.
> d) It rotates the grid 60 degrees.
>> [!success]- Answer
>> b) It sets the transparency of the grid lines.

> [!question] Which Pandas function would you use to quickly see the Min, Max, and Mean of the 'Cholesterol' column?
> a) df.summary()
> b) df.info()
> c) df.describe()
> d) df.stats()
>> [!success]- Answer
>> c) df.describe()

> [!question] In the "Car vs Race Track" analogy, what represents the "Car"?
> a) The Database
> b) The Model/Algorithm
> c) The Infrastructure
> d) The Raw Data
>> [!success]- Answer
>> b) The Model/Algorithm

> [!question] What is the role of the `scripts/` folder in the project structure?
> a) It is mandatory for all projects.
> b) It stores the virtual environment.
> c) It stores pure Python (`.py`) scripts for automation.
> d) It holds the raw CSV data.
>> [!success]- Answer
>> c) It stores pure Python (`.py`) scripts for automation.

> [!question] If you wanted to check if the dataset is balanced between "Male" and "Female", which command should you run?
> a) df['Sex'].mean()
> b) df['Sex'].value_counts()
> c) df['Sex'].describe()
> d) plt.plot(df['Sex'])
>> [!success]- Answer
>> b) df['Sex'].value_counts()

> [!question] Match the library import alias to the library name.
>> [!example] Group A
>> a) pd
>> b) plt
>> c) np
>> d) sns
>
>> [!example] Group B
>> n) seaborn
>> o) pandas
>> p) matplotlib.pyplot
>> q) numpy
>
>> [!success]- Answer
>> a) -> o)
>> b) -> p)
>> c) -> q)
>> d) -> n)

> [!question] Match the data column to its likely data type in Pandas.
>> [!example] Group A
>> a) Age
>> b) Sex
>> c) Cholesterol
>> d) Diagnosis
>
>> [!example] Group B
>> n) Numerical (Integer/Float)
>> o) Categorical (String/Object)
>> p) Numerical (Integer/Float)
>> q) Categorical (String/Object)
>
>> [!success]- Answer
>> a) -> n)
>> b) -> o)
>> c) -> p)
>> d) -> q)

> [!question] Match the Matplotlib function to its specific purpose in the notebook.
>> [!example] Group A
>> a) plt.figure(figsize=(8, 5))
>> b) plt.xlabel()
>> c) plt.ylabel()
>> d) plt.title()
>
>> [!example] Group B
>> n) Labels the Y-axis "Cholesterol (mg/dL)".
>> o) Sets the dimensions of the chart area.
>> p) Adds a main header to the plot.
>> q) Labels the X-axis "Age (Years)".
>
>> [!success]- Answer
>> a) -> o)
>> b) -> q)
>> c) -> n)
>> d) -> p)

> [!question] Match the Virtual Environment concept to its description.
>> [!example] Group A
>> a) Global Installation
>> b) Virtual Environment
>> c) Sandbox
>
>> [!example] Group B
>> n) An isolated area where changes don't affect the system.
>> o) Installing libraries for the entire computer (System-wide).
>> p) A tool that creates an isolated folder for dependencies.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> p)
>> c) -> n)

> [!question] Match the Data Role to their primary "Question".
>> [!example] Group A
>> a) Data Analyst
>> b) Data Scientist
>
>> [!example] Group B
>> n) "What will happen?"
>> o) "What happened?"
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)

> [!question] Match the file to its role in the file system.
>> [!example] Group A
>> a) patients.csv
>> b) patients_analysis.ipynb
>> c) .gitignore
>
>> [!example] Group B
>> n) Configuration file.
>> o) Data file.
>> p) Notebook code file.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> p)
>> c) -> n)

> [!question] Match the logic operation to its outcome in the script.
>> [!example] Group A
>> a) os.path.exists returns True
>> b) os.path.exists returns False
>
>> [!example] Group B
>> n) The script prints "Error: File not found".
>> o) The script proceeds to `pd.read_csv`.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)

> [!question] Match the plot style argument to its result.
>> [!example] Group A
>> a) marker='o'
>> b) linestyle='none'
>
>> [!example] Group B
>> n) No lines connecting the points.
>> o) Points appear as circles.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)

> [!question] Match the folder to the type of file it should contain.
>> [!example] Group A
>> a) data/
>> b) scripts/
>> c) notebooks/
>
>> [!example] Group B
>> n) .ipynb files
>> o) .py files
>> p) .csv files
>
>> [!success]- Answer
>> a) -> p)
>> b) -> o)
>> c) -> n)

> [!question] Match the Git benefit to its explanation.
>> [!example] Group A
>> a) History
>> b) Collaboration
>
>> [!example] Group B
>> n) Pushing code to GitHub for the team.
>> o) Creating a timeline of commits.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)