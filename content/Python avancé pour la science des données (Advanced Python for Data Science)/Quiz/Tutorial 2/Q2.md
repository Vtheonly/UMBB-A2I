---
sources:
  - "[[Tutorial 02 Hospital Patient Data Analysis]]"
---

> [!question] In the project structure, the `scripts/` folder is mandatory for every project and must contain Jupyter Notebooks.
>> [!success]- Answer
>> False

> [!question] The command `df['Column'].value_counts()` is primarily used to analyze categorical data, such as Gender or Diagnosis.
>> [!success]- Answer
>> True

> [!question] In a scatter plot created with `plt.plot`, removing `linestyle='none'` will result in the data points being connected by a line.
>> [!success]- Answer
>> True

> [!question] A Data Analyst focuses on building the "race track" (infrastructure) so the Data Scientist can drive the "car" (models).
>> [!success]- Answer
>> False

> [!question] The `env/` folder is usually added to `.gitignore` because it contains heavy binary files that can be easily recreated.
>> [!success]- Answer
>> True

> [!question] The command `pip install pandas` should be run globally on your computer to ensure all projects share the same version.
>> [!success]- Answer
>> False

> [!question] In the Jupyter Notebook, the `os.path.exists(file_path)` check is used to verify if the CSV file exists before attempting to load it.
>> [!success]- Answer
>> True

> [!question] The Pandas function `describe()` calculates the mean and standard deviation for text-based columns like 'Diagnosis'.
>> [!success]- Answer
>> False

> [!question] CSV stands for "Comma Separated Values" and is a standard format for exchanging simple data.
>> [!success]- Answer
>> True

> [!question] If you clone the project on a new machine, you must manually copy the `env` folder from the previous machine for it to work.
>> [!success]- Answer
>> False

> [!question] What is the output of the "50%" row in the `df.describe()` summary?
> a) The mean average of the data.
> b) The maximum value in the first half of the data.
> c) The median value.
> d) The standard deviation.
>> [!success]- Answer
>> c) The median value.

> [!question] Why do we use `plt.grid(True)` in the visualization code?
> a) To connect the data points with lines.
> b) To add a background grid for easier reading of values.
> c) To save the image to the disk.
> d) To change the color of the markers to blue.
>> [!success]- Answer
>> b) To add a background grid for easier reading of values.

> [!question] Which command creates a new virtual environment named "env"?
> a) pip install env
> b) python -m venv env
> c) source activate env
> d) git init env
>> [!success]- Answer
>> b) python -m venv env

> [!question] According to the text, why can't a Data Analyst usually replace a Data Scientist?
> a) Analysts only know Excel, not Python.
> b) Analysts focus on descriptive statistics of the past, while Scientists predict the future.
> c) Analysts do not understand business logic.
> d) Scientists are better at visualization.
>> [!success]- Answer
>> b) Analysts focus on descriptive statistics of the past, while Scientists predict the future.

> [!question] In the path `data/patients.csv`, what does the `data/` part represent?
> a) The file extension.
> b) A directory dedicated to storing raw datasets.
> c) A python library name.
> d) The virtual environment.
>> [!success]- Answer
>> b) A directory dedicated to storing raw datasets.

> [!question] What is the primary purpose of the `.gitignore` file?
> a) To delete unused files automatically.
> b) To list libraries that need to be installed.
> c) To tell Git which files to exclude from version control.
> d) To store passwords and secrets.
>> [!success]- Answer
>> c) To tell Git which files to exclude from version control.

> [!question] Which line of code correctly loads the 'Sex' column from the dataframe `df`?
> a) df.load('Sex')
> b) df['Sex']
> c) df.describe('Sex')
> d) df.columns('Sex')
>> [!success]- Answer
>> b) df['Sex']

> [!question] In the analogy "The Data Engineer builds the race track", what does the "race track" represent?
> a) The machine learning model.
> b) The visualization dashboard.
> c) The data infrastructure and pipelines.
> d) The Jupyter Notebook.
>> [!success]- Answer
>> c) The data infrastructure and pipelines.

> [!question] If `df.describe()` shows a "Max" Age of 150, what does the tutorial suggest this indicates?
> a) The data is dirty or contains an outlier.
> b) The patient is a world record holder.
> c) The code is broken.
> d) The median is calculated incorrectly.
>> [!success]- Answer
>> a) The data is dirty or contains an outlier.

> [!question] Which library is imported to handle file system paths in the notebook?
> a) sys
> b) numpy
> c) os
> d) paths
>> [!success]- Answer
>> c) os

> [!question] Match the visualization parameter to its function.
>> [!example] Group A
>> a) plt.title()
>> b) plt.xlabel()
>> c) plt.show()
>
>> [!example] Group B
>> n) Renders the final plot to the screen.
>> o) Adds a text label to the horizontal axis.
>> p) Adds a main heading to the chart.
>
>> [!success]- Answer
>> a) -> p)
>> b) -> o)
>> c) -> n)

> [!question] Match the Data Science role to its specific "Time Orientation".
>> [!example] Group A
>> a) Data Analyst
>> b) Data Scientist
>
>> [!example] Group B
>> n) The Future ("What will happen?").
>> o) The Past ("What happened?").
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)

> [!question] Match the command to the Operating System for activating an environment.
>> [!example] Group A
>> a) Windows
>> b) Mac / Linux
>
>> [!example] Group B
>> n) source env/bin/activate
>> o) .\env\Scripts\activate
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)

> [!question] Match the Panda function to the type of data it is best suited for.
>> [!example] Group A
>> a) df.describe()
>> b) value_counts()
>
>> [!example] Group B
>> n) Categorical Data (e.g., Male/Female).
>> o) Numerical Data (e.g., Age, BP).
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)

> [!question] Match the Git concept to its definition.
>> [!example] Group A
>> a) Repository
>> b) Commit
>> c) .gitignore
>
>> [!example] Group B
>> n) A specific snapshot or point in the history timeline.
>> o) A configuration file to exclude files.
>> p) The project folder tracked by Git.
>
>> [!success]- Answer
>> a) -> p)
>> b) -> n)
>> c) -> o)

> [!question] Match the code snippet to the library it belongs to.
>> [!example] Group A
>> a) .read_csv()
>> b) .plot()
>> c) .path.exists()
>
>> [!example] Group B
>> n) matplotlib.pyplot
>> o) os
>> p) pandas
>
>> [!success]- Answer
>> a) -> p)
>> b) -> n)
>> c) -> o)

> [!question] Match the folder name to its content description.
>> [!example] Group A
>> a) notebooks/
>> b) env/
>> c) scripts/
>
>> [!example] Group B
>> n) Isolated library installation sandbox.
>> o) Optional folder for pure .py automation files.
>> p) Folder for Jupyter exploration files.
>
>> [!success]- Answer
>> a) -> p)
>> b) -> n)
>> c) -> o)

> [!question] Match the statistical term to its DataFrame representation.
>> [!example] Group A
>> a) Quartiles
>> b) Average
>> c) Spread/Deviation
>
>> [!example] Group B
>> n) mean
>> o) std
>> p) 25%, 50%, 75%
>
>> [!success]- Answer
>> a) -> p)
>> b) -> n)
>> c) -> o)

> [!question] Match the plot customization to the value used in the notebook.
>> [!example] Group A
>> a) Color
>> b) Marker style
>> c) Line style
>
>> [!example] Group B
>> n) 'none'
>> o) 'b' (blue)
>> p) 'o' (dots)
>
>> [!success]- Answer
>> a) -> o)
>> b) -> p)
>> c) -> n)

> [!question] Match the problem to the solution provided in the tutorial.
>> [!example] Group A
>> a) Dependency Conflicts
>> b) Breaking the code
>> c) Accidental data deletion
>
>> [!example] Group B
>> n) Use Git (Version Control).
>> o) Separate `data/` folder from code.
>> p) Use a Virtual Environment.
>
>> [!success]- Answer
>> a) -> p)
>> b) -> n)
>> c) -> o)